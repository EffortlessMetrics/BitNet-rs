# This file ensures the models/ directory structure is preserved in git
# while preventing large model binaries from being committed.
# Models are downloaded via: cargo run -p xtask -- download-model
