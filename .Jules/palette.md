## 2024-05-22 - Tab Accessibility
**Learning:** Converting `div` tabs to `button` elements requires careful CSS resets (background, border, padding, font-size) to match the original design, especially when overriding generic button styles.
**Action:** Use high-specificity selectors (e.g., `.tabs button`) and explicit properties to reset button styles when retrofitting accessibility.
