<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-053 | #5934 | `codex/slm-cpu-053-packed-q8-runtime-gate` | Add the first evidence-scoped packed Q8_0 sidecar runtime execution proof after SLM-CPU-052. The slice may route only the exact proven dense-linear tensor or minimal tensor set through packed Q8_0 sidecar compute when selector-update proof is supplied, while preserving the eager F32 Candle path as the default and preserving the Qwen3 Q8_0 appliance behavior oracle. It must emit or validate before/after receipts proving identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, fallback_used=false, and speedup_claim=false before claiming even a bounded runtime improvement. If runtime execution cannot be enabled safely, the slice must instead record the precise blocker and keep packed sidecar compute disabled. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes. |
