id = "apple-m4-local-answer"
title = "Apple M4 local answer usability"
status = "active"

objective = "Make Apple Silicon useful for a local Mac user by turning the completed proof and operational lanes into prompt-in, intelligible-answer-out behavior with truthful hardware routing and receipts."

end_state = [
  "apple-m4-cpu-neon can run a supported real GGUF with real tokenizer and strict loader for multi-token local answers.",
  "Short prompt suites produce non-empty, valid UTF-8, non-degenerate generated text under deterministic greedy settings.",
  "Greedy output is stable for the same model, prompt, tokenizer, backend, and runtime settings.",
  "Local-answer receipts record requested backend, selected backend, runtime API, model, tokenizer, kernel family, execution phase, fallback status, generated text, token counts, and timing.",
  "Strict failure modes are predictable when model files, tokenizer authority, backend identity, or unsupported Apple lanes are wrong.",
  "Any future Metal contribution is phase-level and receipt-backed; CPU fallback is visible and never counted as Metal inference.",
]

hard_constraints = [
  "Do not reopen the completed apple-m4 or apple-m4-operational campaigns.",
  "Start with apple-m4-cpu-neon as the reliable local-answer path.",
  "Do not claim coherent local answers from an artifact that is not answer_ready under docs/model-artifacts/ANSWER_ARTIFACT_GATE.md.",
  "Do not claim full apple-m4-metal model inference unless a strict real-model receipt proves it.",
  "Do not claim Neural Engine execution from MPSGraph.",
  "Do not claim QK256 on Apple Silicon.",
  "Do not hide CPU fallback or treat fallback as acceleration.",
  "Do not add broad benchmark or performance claims before quality and receipt gates exist.",
]

[[work_item]]
id = "M4-QA-001"
status = "merged"
pr = 4618
merge_sha = "9fa3e532b89c36885c54350700d75a740a93911e"
branch = "codex/apple-m4-local-answer/M4-QA-001-output-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a multi-prompt Apple M4 CPU/NEON local-answer smoke suite that runs the answer_ready Microsoft I2_S GGUF and external tokenizer under strict mode, uses the bitnetcpp-answer prompt authority, requires every committed answer-corpus gate to pass with valid UTF-8, non-empty output, token IDs, real model SHA/tokenizer authority, prompt-prefill evidence, and explicit fallback=false receipt fields."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m4",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 900 --fail-on-quality --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-full-release.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- mac bitnet-proof --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --proof-receipt ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-full-release.json --strict --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/mac-bitnet-proof-receipt-check.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "ci/hardware/apple-m4-mac-mini/**",
  "ci/quality/**",
  "docs/hardware/apple-m4-mac-mini-operator-runbook.md",
  "docs/slm/apple-m4-bitnet-proof-prep.md",
  "docs/slm/apple-m4-dense-slm-model-support-matrix.md",
  "docs/slm/apple-m4-local-server-command-config.md",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/slm/apple-m4-slm-local-answer-baseline.md",
  "docs/specs/apple-m4-mac-mini-roadmap.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Apple M4 CPU/NEON local-answer smoke coverage exists for the committed strict BitNet answer corpus when the release proof receipt passes.",
]
must_not_claim = [
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "The output-quality smoke suite proves broad benchmark performance.",
  "BitNet is selectable through `bitnet mac ask` or `bitnet mac chat` before that user-facing route is wired and separately checked.",
]

[[work_item]]
id = "M4-QA-ROOT-001"
status = "merged"
pr = 3908
merge_sha = "948c9201aaebc9636c169a805d2a707218dab017"
branch = "codex/apple-m4-local-answer/M4-QA-ROOT-001-bitnetcpp-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Compare the same real GGUF, tokenizer, prompt template, prompt, and greedy settings against bitnet.cpp/reference behavior; either produce token/logit parity evidence for the first divergence and fix the Rust path, or prove the local GGUF artifact itself also garbles under the reference implementation."
commands = [
  "cargo fmt --all -- --check",
  "Reference bitnet.cpp run for the same model, tokenizer, prompt, and greedy settings",
  "BitNet-rs strict run for the same model, tokenizer, prompt, and greedy settings",
  "Token/logit parity comparison or documented reference-output artifact",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-sys/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/**/tests/**",
  "crossval/**",
  "xtask/**",
  "docs/reports/**",
  "docs/tdd/plans/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "The first strict real-output divergence is localized against bitnet.cpp/reference evidence, or the local model artifact is proven to garble in the reference path too.",
]
must_not_claim = [
  "Apple M4 CPU/NEON local answers are coherent until M4-QA-001 passes.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon unless this item specifically proves it for Apple receipt-backed paths.",
  "Neural Engine execution is proven.",
]

[[work_item]]
id = "M4-QA-MODEL-001"
status = "merged"
pr = 3913
merge_sha = "b09bf8555e5861914be9c4d9715c9753636b9e94"
branch = "codex/apple-m4-local-answer/M4-QA-MODEL-001-artifact-validation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-ROOT-001"]
acceptance = "Validate or replace the supported local-answer model artifact so a reference runner produces coherent short answers for the campaign prompt suite before M4-QA-001 is allowed to claim Apple M4 CPU/NEON local-answer smoke coverage."
commands = [
  "cargo fmt --all -- --check",
  "Reference runner command for the supported model artifact and campaign prompt suite",
  "Metadata check that BPE GGUF artifacts include required tokenizer pre-tokenizer metadata or an explicit compatibility decision",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "ci/quality/**",
  "docs/reports/**",
  "docs/tdd/plans/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "A supported local-answer model artifact is reference-validated for the campaign prompt suite, or the current artifact is explicitly rejected with evidence.",
]
must_not_claim = [
  "Apple M4 CPU/NEON local answers are coherent until M4-QA-001 passes.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
]

[[work_item]]
id = "M4-QA-MODEL-002"
status = "merged"
branch = "codex/apple-m4-local-answer/M4-QA-MODEL-002-known-good-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-MODEL-001"]
acceptance = "Acquire or regenerate a supported local-answer GGUF/tokenizer artifact that passes the campaign prompt suite under a reference runner, records exact SHA256 and tokenizer metadata, and is allowed to unblock M4-QA-001; the shared successor gate is MODEL-ARTIFACT-002."
commands = [
  "cargo fmt --all -- --check",
  "Reference runner command for the accepted model artifact and campaign prompt suite",
  "Metadata check that the artifact includes tokenizer pre-tokenizer authority or an explicit compatibility decision",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "ci/quality/**",
  "docs/reports/**",
  "docs/tdd/plans/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "A supported local-answer model artifact is reference-validated for the campaign prompt suite and can be used to rerun M4-QA-001.",
]
must_not_claim = [
  "Apple M4 CPU/NEON local answers are coherent until M4-QA-001 passes.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
]

[[work_item]]
id = "M4-QA-002"
status = "merged"
merge_sha = "9fa3e532b89c36885c54350700d75a740a93911e"
branch = "codex/apple-m4-local-answer/M4-QA-002-greedy-determinism"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-001"]
acceptance = "Add deterministic greedy local-answer checks so the same model, prompt, tokenizer, backend, and runtime settings produce stable token IDs and receipt identity."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m4",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 900 --dump-logit-steps 1 --logits-topk 10 --fail-on-quality --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-determinism-a.json",
  "target/release/bitnet answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 900 --dump-logit-steps 1 --logits-topk 10 --fail-on-quality --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-determinism-b.json",
  "target/release/bitnet answer-parity --left ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-determinism-a.json --right ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-determinism-b.json --left-label apple-m4-run-a --right-label apple-m4-run-b --machine apple-m4-mac-mini --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-local-answer/bitnet-answer-corpus-repeat-parity.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "ci/hardware/apple-m4-mac-mini/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Greedy repeat parity passed for the committed five-case Apple M4 CPU/NEON BitNet answer corpus under fixed model, tokenizer, prompt template, backend, and runtime settings.",
]
must_not_claim = [
  "General BitNet chat quality is proven.",
  "BitNet is selectable through `bitnet mac ask`, `bitnet mac chat`, or `bitnet mac serve`.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "MacBook coverage or broad performance is proven.",
]

[[work_item]]
id = "M4-QA-003"
status = "merged"
merge_sha = "9fa3e532b89c36885c54350700d75a740a93911e"
branch = "codex/apple-m4-local-answer/M4-QA-003-receipt-quality"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-001"]
acceptance = "Harden local-answer receipts so generated text, token counts, tokenizer authority, model identity, backend routing, fallback status, and timing fields are present and checked."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests answer_corpus_",
  "cargo test --locked -p bitnet-cli answer_receipt_",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m4",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "xtask/src/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "The committed Apple M4 CPU/NEON BitNet answer-corpus receipt-quality contract requires and checks generated text, token counts and ID consistency, tokenizer pretokenizer authority, model identity, backend routing, fallback=false, and timing fields for the fixed strict answer corpus.",
]
must_not_claim = [
  "BitNet is selectable through `bitnet mac ask`, `bitnet mac chat`, or `bitnet mac serve`.",
  "The fixed answer-corpus receipt-quality checks prove broad BitNet chat quality.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad performance or speedup is proven.",
]

[[work_item]]
id = "M4-QA-004"
status = "merged"
merge_sha = "9fa3e532b89c36885c54350700d75a740a93911e"
branch = "codex/apple-m4-local-answer/M4-QA-004-strict-failure-modes"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-001"]
acceptance = "Add strict local-answer failure-mode coverage for missing model files, tokenizer authority failures, unsupported Apple backend selections, and attempts to count fallback as acceleration."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests answer_corpus_",
  "cargo test --locked -p bitnet-cli answer_receipt_",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m4",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Strict local-answer failure-mode coverage exists for missing model files, missing tokenizer authority, missing tokenizer files, unsupported Apple Metal/MPSGraph answer-corpus selections, hidden fallback, and speedup/full-inference claim rejection.",
]
must_not_claim = [
  "BitNet is selectable through `bitnet mac ask`, `bitnet mac chat`, or `bitnet mac serve`.",
  "The strict failure-mode tests prove broad BitNet chat quality.",
  "Full apple-m4-metal model inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad performance or speedup is proven.",
]

[[work_item]]
id = "M4-QA-005"
status = "merged"
merge_sha = "9fa3e532b89c36885c54350700d75a740a93911e"
branch = "codex/apple-m4-local-answer/M4-QA-005-metal-phase-routing"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-002", "M4-QA-003"]
acceptance = "Decide and document the first safe route for one receipt-backed Apple Metal phase to participate in real generation while preserving CPU fallback visibility and greedy output comparison."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/hardware/apple-m4-mac-mini-operator-runbook.md",
  "docs/specs/apple-m4-mac-mini-roadmap.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The first eligible future Apple M4 BitNet Metal local-answer contribution has been documented as a prefill projection fixture with CPU-only greedy reference comparison, CPU/Metal phase parity, unchanged generated token IDs and decoded text, and explicit fallback=false phase receipts.",
]
must_not_claim = [
  "A BitNet Metal local-answer route is enabled today.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "BitNet is selectable through `bitnet mac ask`, `bitnet mac chat`, or `bitnet mac serve`.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad performance or speedup is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-000"
status = "merged"
pr = 4637
merge_sha = "d8893da79e2e1a792b8bba7ba6fb14ae7924a2a7"
branch = "codex/apple-m4-bitnet-one-shot-ask"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-QA-001", "M4-QA-002", "M4-QA-003", "M4-QA-004"]
acceptance = "Wire an explicit BitNet one-shot `bitnet mac ask` route that is gated by the accepted Microsoft I2_S GGUF SHA and external tokenizer SHA, marks the catalog row as supported-ask, keeps BitNet chat and serve disabled, and does not claim a fresh runtime smoke receipt."
commands = [
  "cargo fmt --all -- --check",
  "git diff --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_ask_",
  "cargo run --locked -p xtask --no-default-features -- check-no-panic-family --report-dir target/bitnet/reports",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "gh pr checks 4637",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/hardware/apple-m4-mac-mini-operator-runbook.md",
  "docs/slm/apple-m4-bitnet-proof-prep.md",
  "docs/slm/apple-m4-local-server-command-config.md",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/slm/apple-m4-slm-local-answer-baseline.md",
  "docs/specs/apple-m4-mac-mini-roadmap.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "BitNet has an explicit one-shot `bitnet mac ask` route gated by accepted artifact and tokenizer identity.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "A fresh BitNet `bitnet mac ask` runtime proof completed.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad performance or speedup is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-001"
status = "merged"
pr = 4647
merge_sha = "8a0d463ba0e5be69d5c382a050e0d8a034f7fd38"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-001-runtime-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-000"]
acceptance = "Prove the user-facing `bitnet mac ask` BitNet route completes through the accepted Microsoft I2_S GGUF and accepted external tokenizer in strict mode, selects apple-m4-cpu-neon, records fallback_used=false, valid UTF-8, non-empty coherent output, generated token IDs, timing receipt fields, and a documented operator command; if the route is too slow, record load, tokenizer, prefill, first-token, decode timing, timeout boundary, and where the delay occurs without claiming success."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- --device apple-m4-cpu-neon mac ask --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --max-new-tokens 8 --json-out ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-mac-ask/bitnet-mac-ask-runtime-receipt.json \"What is 2+2? Answer briefly.\"",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-mac-ask/bitnet-mac-ask-runtime-receipt.json",
  "jq -e '.artifact_kind == \"strict_bitnet_cpu_profile\" and .requested_backend == \"apple-m4-cpu-neon\" and .selected_backend == \"apple-m4-cpu-neon\" and .runtime_api == \"cpu\" and .fallback_used == false and .model.sha256 == \"4221b252fdd5fd25e15847adfeb5ee88886506ba50b8a34548374492884c2162\" and .tokenizer.strict == true and .tokenizer.pretokenizer_authority == \"llama-bpe\" and (.text | type == \"string\" and length > 0) and (.tokens.generated_ids | type == \"array\") and ((.tokens.generated_ids | length) == .tokens.generated) and .mac_bitnet_claim_boundary.chat_enabled == false and .mac_bitnet_claim_boundary.serve_enabled == false and .mac_bitnet_claim_boundary.full_metal_inference_claimed == false' ci/hardware/apple-m4-mac-mini/2026-05-13/bitnet-mac-ask/bitnet-mac-ask-runtime-receipt.json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_ask_bitnet",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "ci/hardware/apple-m4-mac-mini/**",
  "docs/hardware/apple-m4-mac-mini-operator-runbook.md",
  "docs/slm/apple-m4-bitnet-proof-prep.md",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/slm/apple-m4-slm-local-answer-baseline.md",
  "docs/specs/apple-m4-mac-mini-roadmap.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "The user-facing BitNet one-shot `bitnet mac ask` route completed for the committed single-prompt Apple M4 CPU/NEON receipt with text `2+2 equals 4.`, generated token IDs, timing fields, accepted model/tokenizer identity, and fallback_used=false.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-002"
status = "merged"
pr = 4651
merge_sha = "e874ca85d92183d4131ab11a94d53688b1ab3a48"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-002-progress"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-001"]
acceptance = "Add explicit progress/status UX for slow one-shot `bitnet mac ask` runs so operators can see tokenizer/model verification, model/tokenizer loading, prompt tokenization, prefill, first-token, decode completion, and receipt validation milestones on stderr while generated text remains on stdout; add `--quiet` suppression and keep BitNet chat/serve/Metal claim boundaries unchanged."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_ask",
  "cargo test --locked -p bitnet-cli --test cli_snapshot_tests mac_ask_help",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "One-shot `bitnet mac ask` supports explicit operator progress/status milestones on stderr, with `--quiet` suppression for scripts.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-003"
status = "merged"
pr = 4680
merge_sha = "f19375062636d436ca7005de140e5df6f544ea39"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-003-failure-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-002"]
acceptance = "Add partial-failure receipts for one-shot BitNet `bitnet mac ask` setup and generation failures so missing tokenizer, tokenizer SHA mismatch, missing/wrong accepted GGUF, and generation errors leave a durable receipt with failure stage, repair guidance, timeout-boundary status, fallback_used=false, empty partial generation, and unchanged BitNet chat/serve/Metal claim boundaries."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_ask_bitnet",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "One-shot BitNet `bitnet mac ask` writes durable partial-failure receipts for setup and generation failures before returning an error.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-004"
status = "merged"
pr = 4686
merge_sha = "a38c63fb13764b2265b97d4906362b568bb704c0"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-004-repair-guidance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-003"]
acceptance = "Print compact operator repair guidance on BitNet `bitnet mac ask` setup and generation failures while preserving durable failure receipts: missing tokenizer authority/path, tokenizer SHA mismatch, missing/wrong accepted GGUF, cache repair, and generation errors should surface actionable accepted-tokenizer, model fetch/verify, explicit GGUF verify, `bitnet mac models`, and unchanged BitNet chat/serve/Metal claim-boundary guidance in stderr and receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_ask_bitnet",
  "cargo test --locked -p bitnet-cli --bin bitnet bitnet_mac_ask_model_failure_guidance",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "One-shot BitNet `bitnet mac ask` setup and generation failures print compact repair guidance that matches the durable failure receipt guidance.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-005"
status = "merged"
pr = 4688
merge_sha = "d67519d2547ec5b65d46594f8f191c068027f662"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-005-doctor-readiness"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-004"]
acceptance = "Add advisory BitNet one-shot ask readiness into `bitnet mac doctor` receipts so operators can see the supported-ask catalog row, cached model readiness, accepted external tokenizer path/SHA status, fetch/verify/model commands, example cached ask command, and unchanged BitNet chat/serve/Metal claim boundaries without making dense SLM doctor fail when optional BitNet artifacts are absent."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_doctor",
  "cargo test --locked -p bitnet-cli --bin bitnet bitnet_mac_ask_readiness",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "`bitnet mac doctor` receipts include advisory BitNet one-shot ask readiness for cache/tokenizer/catalog repair without enabling BitNet chat or serve.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-ASK-006"
status = "merged"
pr = 4691
merge_sha = "c0d524fac23c556c0ce726c17d029b88cfec1af7"
branch = "codex/apple-m4-local-answer/M4-BITNET-ASK-006-bitnet-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-005"]
acceptance = "Add `bitnet mac smoke --model-family bitnet` as a narrow one-shot BitNet ask smoke that defaults to the accepted Microsoft I2_S model id and external tokenizer path, preserves explicit --model-path/--tokenizer overrides only for BitNet, writes a compact aggregate smoke receipt after a successful one-shot answer, leaves durable BitNet ask failure receipts on setup failures, and keeps BitNet chat, serve, full Metal, QK256, Neural Engine, MPSGraph, broad quality, and performance claims disabled."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_smoke",
  "cargo test --locked -p bitnet-cli --test cli_snapshot_tests mac_smoke_help",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "`bitnet mac smoke --model-family bitnet` runs the explicit one-shot BitNet ask route when accepted artifacts are available and leaves setup failure receipts when they are not.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-SMOKE-001"
status = "merged"
pr = 4697
merge_sha = "a4d500d10ef4d8447e815a0ddd95acc4cdfc6ee2"
head_sha = "67e55161f490d4cd805f512f7893eefcb673d61f"
branch = "codex/apple-m4-local-answer/M4-BITNET-SMOKE-001-runtime-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-ASK-006"]
acceptance = "Commit a runtime receipt for `bitnet mac smoke --model-family bitnet` using the accepted Microsoft I2_S GGUF and accepted external tokenizer under strict apple-m4-cpu-neon routing, with fallback_used=false, expected text fragment found, generated text, generated token IDs, timing fields, receipt validation, documented operator command, and chat, serve, full Metal, QK256, Neural Engine, MPSGraph, broad quality, and broad performance claims disabled."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- --device apple-m4-cpu-neon mac smoke --model-family bitnet --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --max-new-tokens 8 --json-out ci/hardware/apple-m4-mac-mini/2026-05-14/bitnet-mac-smoke/bitnet-mac-smoke-runtime-receipt.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-14/bitnet-mac-smoke/bitnet-mac-smoke-runtime-receipt.json --json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-14/bitnet-mac-smoke/bitnet-mac-smoke-runtime-receipt-answer.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "`bitnet mac smoke --model-family bitnet` completed for the committed Apple M4 CPU/NEON one-shot smoke receipt with accepted model/tokenizer identity, fallback_used=false, generated text, generated token IDs, and disabled chat/serve/Metal boundaries.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
  "Broad BitNet performance is proven.",
]

[[work_item]]
id = "M4-BITNET-WARM-001"
status = "merged"
pr = 4701
merge_sha = "fa2c7c2d459a38ae19d5fdf269d7d8e8d96cf2b0"
head_sha = "93dc74d5159b0b401f8a01f79099cea4cf89c846"
branch = "codex/apple-m4-local-answer/M4-BITNET-WARM-001-route"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-SMOKE-001"]
acceptance = "Add a narrow `bitnet mac bitnet-warm` route that uses the accepted Microsoft I2_S GGUF and accepted external tokenizer, loads model/tokenizer once through the resident warm-session engine, runs three fixed prompts with one repeated prompt for determinism, writes per-turn receipts and an aggregate `bitnet_apple_m4_warm_session` receipt, records fallback_used=false, generated text/token IDs, timing and memory fields, validates the receipt, and keeps BitNet chat, serve, full Metal, QK256, Neural Engine, MPSGraph, broad quality, and broad performance claims disabled."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_bitnet_warm",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_receipts_check_accepts_bitnet_warm_session_receipt",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- --device apple-m4-cpu-neon mac bitnet-warm --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --max-new-tokens 8 --json-out ci/hardware/apple-m4-mac-mini/2026-05-14/bitnet-warm/bitnet-mac-bitnet-warm-runtime-receipt.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-14/bitnet-warm/bitnet-mac-bitnet-warm-runtime-receipt.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "ci/hardware/apple-m4-mac-mini/**",
  "docs/slm/apple-m4-mini-user-expectation-envelope.md",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "`bitnet mac bitnet-warm` completed a fixed-prompt Apple M4 CPU/NEON BitNet warm-session receipt with accepted model/tokenizer identity, one model/tokenizer load, per-turn receipts, repeated-prompt determinism, fallback_used=false, generated text, generated token IDs, and disabled chat/serve/Metal boundaries.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet chat quality or performance is proven.",
  "Broad BitNet warm-session quality or performance is proven.",
]

[[work_item]]
id = "M4-BITNET-WARM-002"
status = "merged"
branch = "codex/apple-m4-local-answer/M4-BITNET-WARM-002-catalog-readiness"
pr = 4705
head_sha = "715252ea8f8cb41128f045e70b5242904bde15ed"
merge_sha = "0cb4a1cdd73e7ec4cccdb92054e101a9feb37ce0"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-WARM-001"]
acceptance = "Update the Apple M4 operator catalog and readiness surfaces so the accepted BitNet row exposes the fixed-prompt `bitnet mac bitnet-warm` route alongside one-shot ask: `bitnet mac models` JSON/text include warm capability, warm command, warm receipt path, and unchanged chat/serve disabled flags; `bitnet mac doctor` advisory BitNet readiness includes a cached warm command and warm claim boundary; local-server `/models` and `mac serve-check` validate the warm catalog fields without treating BitNet as a server completion model; docs/tracking explain the route as fixed-prompt warm proof only."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_models",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_doctor_missing_cache_points_to_model_fetch_and_writes_receipt",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_check_rejects_blocked_bitnet_model_before_cache_guidance",
  "cargo test --locked -p bitnet-cli mac_serve_http_models_route_reports_catalog_no_generation",
  "cargo test --locked -p bitnet-cli bitnet_mac_ask_readiness_is_advisory_and_preserves_claim_boundary",
  "cargo test --locked -p bitnet-cli mac_serve_check_models_catalog_pass_requires_operator_catalog_shape",
  "cargo test --locked -p bitnet-cli --test cli_snapshot_tests mac_smoke_help",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-local-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-cli/src/model_cache.rs",
  "crates/bitnet-cli/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "`bitnet mac models`, `bitnet mac doctor`, and local-server `/models` expose the accepted BitNet fixed-prompt warm route as catalog/readiness metadata with committed warm receipt path and chat/serve disabled boundaries.",
]
must_not_claim = [
  "BitNet chat works.",
  "BitNet serve works.",
  "Full apple-m4-metal model inference works.",
  "Apple M4 Metal participates in autoregressive BitNet decode.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "MPSGraph inference is proven.",
  "Broad BitNet warm-session quality or performance is proven.",
]
