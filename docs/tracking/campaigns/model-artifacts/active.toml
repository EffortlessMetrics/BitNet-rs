id = "model-artifacts"
title = "Model artifact answer authority"
status = "active"

objective = "Define and maintain shared answer-artifact authority so hardware lanes cannot claim coherent local answers from structurally valid but answer-bad GGUF artifacts."

end_state = [
  "Answer-capable model artifacts are distinguished from structurally valid GGUFs.",
  "Rejected artifacts record exact identity, hash, tokenizer authority, reference runner result, prompt-suite result, and claim boundary.",
  "Hardware answer-readiness lanes depend on an answer_ready artifact before coherent local-answer claims.",
  "Diagnostic-only receipts remain allowed against rejected artifacts when they do not claim answer readiness or speedup.",
]

hard_constraints = [
  "Do not weaken CPU, CUDA, Apple, NPU, SLM, server, or quality gates.",
  "Do not touch kernels, transformer routing, tokenizer behavior, model loader behavior, server inference, or accelerator runtime code in tracker-only PRs.",
  "Do not claim a structurally valid GGUF is answer-ready without reference runner prompt-suite evidence.",
  "Do not turn backend execution proof into answer-readiness proof.",
  "Do not make speed claims from answer artifacts.",
]

[[work_item]]
id = "MODEL-ARTIFACT-001"
status = "merged"
pr = 3922
branch = "codex/model-artifacts/MODEL-ARTIFACT-001-shared-answer-gate-v2"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Define the shared reference-good answer artifact gate, record artifact states, and list the current Microsoft BitNet I2_S GGUF as rejected for answer-readiness claims without changing runtime behavior."
commands = [
  "Parse ci/model-artifacts/*.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
]
may_claim = [
  "A shared answer-artifact gate exists after merge.",
  "The current Microsoft BitNet I2_S GGUF is rejected for coherent local-answer claims after merge.",
]
must_not_claim = [
  "CPU local answers are coherent.",
  "CUDA local answers are coherent.",
  "Apple M4 local answers are coherent.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-002"
status = "blocked"
pr = 3928
branch = "codex/model-artifacts/MODEL-ARTIFACT-002-reference-good-bitnet"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-001"]
acceptance = "Acquire or regenerate a reference-good BitNet GGUF/tokenizer artifact that passes the deterministic answer prompt suite under a reference runner, records exact SHA256 and tokenizer/pre-tokenizer authority, and can unblock backend answer-readiness lanes."
commands = [
  "Reference runner command for the accepted model artifact and prompt suite",
  "Parse ci/model-artifacts/*.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "ci/quality/**",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/tracking/campaigns/apple-m4-local-answer/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "No recorded BitNet GGUF/tokenizer artifact currently passes the shared reference-good answer artifact gate after merge.",
  "Backend answer-readiness lanes remain blocked or diagnostic-only until a future answer_ready artifact is recorded.",
]
must_not_claim = [
  "A reference-good BitNet answer artifact has been acquired.",
  "Any backend local-answer lane passes until that lane reruns its own strict receipt gate.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-003"
status = "merged"
pr = 3932
branch = "codex/model-artifacts/MODEL-ARTIFACT-003-reference-runner-compat"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-002"]
acceptance = "Record reference-runner compatibility evidence for official-derived BitNet GGUF candidates, including the latest stock llama.cpp runner result, without claiming answer readiness or changing runtime behavior."
commands = [
  "Reference-runner compatibility command for tested candidates",
  "Parse ci/model-artifacts/*.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "ci/quality/**",
  "docs/reports/**",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
]
may_claim = [
  "A tested candidate is reference-runner blocked if the recorded reference runner cannot load it.",
  "Latest stock llama.cpp compatibility is insufficient evidence for answer readiness when it fails before prompt execution.",
  "Backend answer-readiness lanes remain blocked or diagnostic-only until an answer_ready artifact is recorded.",
]
must_not_claim = [
  "A reference-good BitNet answer artifact has been acquired.",
  "A candidate that fails reference-runner loading is structurally valid in this runner.",
  "Any CPU or CUDA local-answer lane passes.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-004"
status = "merged"
pr = 3939
branch = "codex/model-artifacts/MODEL-ARTIFACT-004-ikllama-intended-runner"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-003"]
acceptance = "Record intended ik_llama.cpp runner evidence for official-derived BitNet GGUF candidates, including tdh111 IQ2_BN_R4 prompt-suite output and official Microsoft I2_S prompt-suite output, without promoting an answer_ready artifact or changing runtime behavior."
commands = [
  "Intended ik_llama.cpp runner prompt-suite command for tested candidates",
  "Parse ci/model-artifacts/*.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "ci/quality/**",
  "docs/reports/**",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
]
may_claim = [
  "tdh111 IQ2_BN_R4 passes the tiny deterministic prompt suite under its intended ik_llama.cpp runner, subject to documented metadata and scope caveats.",
  "The official Microsoft I2_S artifact still fails the prompt suite under ik_llama.cpp.",
  "No backend answer-readiness lane is unblocked by this evidence.",
]
must_not_claim = [
  "A shared answer_ready BitNet artifact has been acquired.",
  "tdh111 IQ2_BN_R4 satisfies the official Microsoft I2_S CUDA answer target.",
  "CPU or CUDA local-answer generation is coherent in Rust.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-005"
status = "merged"
pr = 3977
branch = "codex/model-artifacts/MODEL-ARTIFACT-005-authority-dimensions"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-004"]
acceptance = "Split artifact authority into explicit target alignment, runner authority, tokenizer/pre-tokenizer authority, prompt-suite result, and per-lane unblock fields so alternate-quant control evidence cannot be confused with the official Microsoft I2_S CUDA target."
commands = [
  "Parse ci/model-artifacts/*.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
]
forbidden_paths = [
  "crates/**",
  "runtime behavior",
  "tokenizer behavior",
  "model loader behavior",
  "transformer/math code",
  "QK256/CUDA",
  "server/NPU/Metal/A770/SLM runtime",
  "speed claims",
  "quality gate weakening",
]
may_claim = [
  "Official Microsoft I2_S remains the official CUDA target and remains blocked.",
  "tdh111 IQ2_BN_R4 is useful alternate-quant control evidence.",
  "tdh111 IQ2_BN_R4 cannot unblock official Microsoft I2_S CUDA answer-readiness.",
  "Missing pre-tokenizer authority remains a first-class blocker.",
]
must_not_claim = [
  "A shared answer_ready BitNet artifact has been acquired.",
  "tdh111 IQ2_BN_R4 satisfies the official Microsoft I2_S CUDA answer target.",
  "CPU or CUDA local-answer generation is coherent in Rust.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-006"
status = "merged"
pr = 3979
branch = "codex/model-artifacts/MODEL-ARTIFACT-006-tokenizer-authority"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-005"]
acceptance = "Audit tokenizer and pre-tokenizer authority for the official Microsoft I2_S target and tdh111 alternate-quant control, including GGUF metadata, external tokenizer assets, prompt-template authority, and whether external authority can unblock official I2_S CUDA answer-readiness."
commands = [
  "Parse ci/model-artifacts/*.toml",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/model-artifacts/**",
]
forbidden_paths = [
  "crates/**",
  "runtime behavior",
  "tokenizer behavior",
  "model loader behavior",
  "transformer/math code",
  "QK256/CUDA",
  "server/NPU/Metal/A770/SLM runtime",
  "speed claims",
  "quality gate weakening",
]
may_claim = [
  "The official Microsoft I2_S GGUF truly lacks tokenizer.ggml.pre metadata.",
  "The source Microsoft tokenizer.json can supply external pre-tokenizer authority for diagnostics.",
  "External tokenizer authority alone does not promote the official I2_S artifact to answer_ready.",
  "tdh111 IQ2_BN_R4 remains alternate-quant control evidence only.",
]
must_not_claim = [
  "A shared answer_ready BitNet artifact has been acquired.",
  "External tokenizer authority makes official I2_S coherent.",
  "CPU or CUDA local-answer generation is coherent in Rust.",
  "Any backend speedup is proven.",
]

[[work_item]]
id = "MODEL-ARTIFACT-007"
status = "pr_open"
pr = 3988
branch = "codex/model-artifacts/MODEL-ARTIFACT-007-msft-bitnetcpp-external-pretokenizer"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["MODEL-ARTIFACT-006"]
acceptance = "Record Microsoft BitNet.cpp reference-runner evidence showing the official Microsoft I2_S GGUF passes the committed deterministic answer corpus when the externally supplied Microsoft tokenizer pre-tokenizer authority is injected with tokenizer.ggml.pre=llama-bpe, without changing Rust runtime behavior or making backend answer claims."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- fetch-cpp --backend cpu",
  "Microsoft BitNet.cpp llama-cli prompt-suite runs with --override-kv tokenizer.ggml.pre=str:llama-bpe --no-mmap",
  "Parse ci/model-artifacts/*.toml",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/model-artifacts/**",
  "ci/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/model-artifacts/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "runtime behavior",
  "tokenizer behavior",
  "model loader behavior",
  "transformer/math code",
  "QK256/CUDA",
  "server/NPU/Metal/A770/SLM runtime",
  "speed claims",
  "quality gate weakening",
]
may_claim = [
  "The official Microsoft I2_S GGUF has reference-runner prompt-suite evidence under Microsoft BitNet.cpp when external tokenizer pre-tokenizer authority is supplied.",
  "The official Microsoft I2_S artifact can unblock backend answer-readiness gates as an answer_ready artifact after merge.",
  "Backend lanes must still rerun their own strict CPU/CUDA/accelerator receipt gates before claiming local-answer success.",
]
must_not_claim = [
  "Rust CPU local-answer generation is coherent.",
  "Rust CUDA local-answer generation is coherent.",
  "Apple M4, NPU, SLM, server, or any other backend answer-readiness is proven.",
  "Any backend speedup is proven.",
]
