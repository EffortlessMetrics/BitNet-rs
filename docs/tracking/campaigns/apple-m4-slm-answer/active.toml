id = "apple-m4-slm-answer"
title = "Apple M4 SLM local answer usability"
status = "active"

objective = "Make the M4 Mac mini produce prompt-in, intelligible-answer-out local runs with a small storage-conscious dense SLM while preserving receipt-backed hardware routing and keeping blocked BitNet local-answer gates honest."

end_state = [
  "A sub-1 GiB dense instruct GGUF is reference-validated for short local-answer prompts.",
  "The selected artifact records source, SHA256, size, GGUF architecture, quantization, tokenizer metadata, pre-tokenizer authority, and prompt template.",
  "apple-m4-cpu-neon can run the validated SLM through the Rust CLI with strict loader/tokenizer behavior and explicit fallback status.",
  "Short prompt suites produce valid UTF-8, non-empty, non-degenerate generated text under deterministic greedy settings.",
  "Warm-session operation loads model and tokenizer once and records timing split by load, tokenize, prefill, decode, sampling, and total time.",
  "Any future Metal contribution is phase-level and receipt-backed; CPU fallback is explicit and never counted as full Metal inference.",
]

hard_constraints = [
  "Do not reopen the completed apple-m4 or apple-m4-operational campaigns.",
  "Do not weaken the blocked BitNet apple-m4-local-answer gates.",
  "Do not claim BitNet local-answer quality from dense SLM evidence.",
  "Do not touch QK256, bitnet-qk256-dispatch, Metal kernels, MPSGraph execution, Neural Engine routing, or server inference for the campaign seed.",
  "Do not claim full apple-m4-metal inference until a strict real-model receipt proves it.",
  "Do not claim general performance from a tiny answer smoke or cold-start run.",
  "Never commit model binaries.",
]

[[work_item]]
id = "SLM-M4-001"
status = "merged"
branch = "codex/apple-m4-slm-answer/SLM-M4-001-seed-campaign"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Seed the Apple M4 SLM answer campaign with objective, end state, storage policy, hard claim boundaries, campaign-local Codex goal, and an ordered work queue whose first ready item validates a small dense instruct GGUF under a reference runner."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".codex/bitnet-alignment.md",
  ".codex/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/TRACKER_MODEL.md",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
  "xtask/src/campaign.rs",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The Apple M4 SLM answer campaign exists after merge.",
  "Small dense SLM work is separated from the blocked BitNet Apple local-answer gate.",
]
must_not_claim = [
  "A dense SLM artifact is accepted.",
  "Apple M4 SLM local answers work.",
  "BitNet local-answer quality is proven.",
  "Full apple-m4-metal inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "General performance is proven.",
]

[[work_item]]
id = "SLM-M4-002"
status = "merged"
branch = "codex/apple-m4-slm-answer/SLM-M4-002-validate-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-M4-001"]
acceptance = "Validate a sub-1 GiB dense instruct GGUF under a reference runner against the M4 SLM prompt suite, record source, SHA256, size, GGUF architecture, quantization, tokenizer metadata, pre-tokenizer authority, prompt template, and reference output, and reject candidates that fail quality."
commands = [
  "cargo fmt --all -- --check",
  "Reference runner command for the accepted SLM artifact and prompt suite",
  "Metadata check for GGUF architecture, tokenizer.ggml.pre, tokenizer.chat_template, and quantization",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "docs/reports/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "A small dense SLM artifact is reference-validated for the Apple M4 SLM prompt suite if the reference runner passes.",
]
must_not_claim = [
  "Rust-native Apple M4 SLM answers work until SLM-M4-003 passes.",
  "BitNet local-answer quality is proven.",
  "Full apple-m4-metal inference works.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "General performance is proven.",
]

[[work_item]]
id = "SLM-M4-003"
status = "pr_open"
branch = "codex/apple-m4-slm-answer/SLM-M4-003-rust-cli-answer"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
pr = 3937
blocked_by = ["SLM-M4-002"]
acceptance = "Run the validated dense SLM through the Rust CLI with apple-m4-cpu-neon, strict loader/tokenizer behavior, explicit fallback status, coherent answer text, generated token IDs, and timing in receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "Manual Apple M4 SLM answer command with receipt artifact",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/**/tests/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Rust-native Apple M4 CPU/NEON SLM answer smoke works if the manual proof command passes.",
]
must_not_claim = [
  "BitNet local-answer quality is proven.",
  "Full apple-m4-metal inference works.",
  "General performance is proven.",
]

[[work_item]]
id = "SLM-M4-004"
status = "proposed"
branch = "codex/apple-m4-slm-answer/SLM-M4-004-warm-session"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-M4-003"]
acceptance = "Add warm-session behavior so the validated model and tokenizer are loaded once, multiple prompts can run in one process, per-prompt receipts are emitted, and model_load, tokenize, prefill, decode, sampling, and total timing are separated."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Warm-session Apple M4 SLM answer flow exists after proof.",
]
must_not_claim = [
  "Broad performance is proven.",
  "Full apple-m4-metal inference works.",
]

[[work_item]]
id = "SLM-M4-005"
status = "proposed"
branch = "codex/apple-m4-slm-answer/SLM-M4-005-quality-determinism"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-M4-003"]
acceptance = "Add a deterministic Apple M4 SLM quality corpus so temperature=0 runs produce stable token IDs and receipts record valid UTF-8, non-empty, non-degenerate answers, model/tokenizer identity, backend routing, fallback status, and timing."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Deterministic Apple M4 SLM quality corpus evidence exists after proof.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Broad performance is proven.",
]

[[work_item]]
id = "SLM-M4-006"
status = "proposed"
branch = "codex/apple-m4-slm-answer/SLM-M4-006-warm-speed"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-M4-004", "SLM-M4-005"]
acceptance = "Measure and improve warm-answer speed by avoiding repeated model/tokenizer loads, reusing buffers and sampling/logits scratch where safe, separating prefill/decode timing, and recording warm tokens/sec without broad performance claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
]
may_claim = [
  "Warm-answer timing is measured for the validated SLM under the recorded machine context.",
]
must_not_claim = [
  "General M4 performance is proven.",
  "BitNet performance is proven.",
]

[[work_item]]
id = "SLM-M4-007"
status = "proposed"
branch = "codex/apple-m4-slm-answer/SLM-M4-007-metal-phase-decision"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-M4-005", "SLM-M4-006"]
acceptance = "Decide the first safe Apple Metal phase contribution for the validated SLM only after CPU/NEON answers are stable; require CPU-only vs CPU+Metal greedy comparison, Metal phase fallback=false, and explicit CPU fallback for the rest."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-answer",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-answer/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "A safe first Metal phase route is selected or explicitly deferred.",
]
must_not_claim = [
  "Full apple-m4-metal inference works.",
  "Neural Engine execution is proven.",
  "General performance is proven.",
]
