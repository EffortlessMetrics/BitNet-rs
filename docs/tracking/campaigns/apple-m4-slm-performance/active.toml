id = "apple-m4-slm-performance"
title = "Apple M4 SLM performance"
status = "active"

objective = "Turn the working Apple M4 SLM answer path into a fast, efficient, repeatable local-answer engine by measuring release-mode warm-session bottlenecks, removing hot-loop overhead, improving CPU/NEON execution, expanding only parity-gated Metal phases, and keeping every speed claim tied to receipts."

end_state = [
  "Release-mode warm-session baselines exist for warm_16, warm_32, warm_64, and warm_128 with cold load separated from warm prompt timing.",
  "Decode-loop allocation and temporary-buffer churn are audited and bounded before math optimization.",
  "Resident sessions are the normal multi-prompt path and emit per-prompt plus aggregate receipts.",
  "CPU/NEON optimizations preserve greedy token IDs and pass the deterministic quality corpus.",
  "Expanded Metal phases are parity-gated, phase-scoped, fallback=false, and do not imply full apple-m4-metal inference.",
  "A measured performance envelope documents only the recorded model, profile, backend, machine context, and receipt-backed timings.",
]

hard_constraints = [
  "Do not reopen the completed apple-m4, apple-m4-operational, apple-m4-slm-answer, or apple-m4-productization campaigns.",
  "Do not weaken the blocked BitNet apple-m4-local-answer gates.",
  "Do not claim BitNet local-answer quality from dense SLM evidence.",
  "Do not claim full apple-m4-metal inference from named Metal phases.",
  "Do not claim Neural Engine execution, MPSGraph model inference, QK256 support, or broad M4 performance.",
  "Do not optimize cold start before release-mode warm-session bottlenecks are measured.",
  "Never commit model binaries.",
  "Prefer the verified local model cache and keep downloads storage-conscious.",
]

[[work_item]]
id = "M4-SLM-PERF-001"
status = "merged"
pr = 4044
merge_sha = "de442a1a5a863c7b5acbbdb26c0a3a69e1b3c4eb"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-001-release-baseline"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a release-mode Apple M4 SLM warm-session baseline for warm_16, warm_32, warm_64, and warm_128 profiles, separating cold load from warm prompt timing and recording model/tokenizer/backend/fallback/session fields without making broad performance claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set performance --json-out target/apple-m4-slm-performance/M4-SLM-PERF-001/release-baseline.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".codex/bitnet-alignment.md",
  ".codex/campaigns/apple-m4-slm-performance/**",
  "Cargo.lock",
  "crates/bitnet-cli/Cargo.toml",
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
  "ci/hardware/apple-m4-mac-mini/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Release-mode warm Apple M4 SLM timing is recorded for the named model, profiles, backend, and machine context.",
  "Cold load and warm prompt timing are separated for the recorded profiles.",
]
must_not_claim = [
  "Broad M4 performance is proven.",
  "BitNet local-answer quality or performance is proven.",
  "Full apple-m4-metal inference works.",
  "A named Metal phase proves full model acceleration.",
]

[[work_item]]
id = "M4-SLM-PERF-002"
status = "merged"
pr = 4047
merge_sha = "eed2e06f30a866037caee07b3ad442297a190dec"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-002-allocation-audit"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-001"]
acceptance = "Audit decode-loop allocations, logits and sampling scratch allocation, token vector growth, detokenization/string churn, receipt construction, and temporary tensor creation; name unavoidable per-token allocations before any math optimization work."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set performance --allocation-audit --json-out ci/hardware/apple-m4-mac-mini/2026-05-08/slm-performance/allocation-audit.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-08/slm-performance/allocation-audit.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
  "ci/hardware/apple-m4-mac-mini/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Apple M4 SLM decode-loop allocation and temporary-buffer hotspots are identified and ranked.",
  "Unavoidable per-token allocations, if any, are named before optimization work starts.",
]
must_not_claim = [
  "CPU/NEON performance is improved.",
  "A specific hot-path optimization is complete.",
  "Broad M4 performance is proven.",
  "Full apple-m4-metal inference works.",
]

[[work_item]]
id = "M4-SLM-PERF-003"
status = "merged"
pr = 4050
merge_sha = "5171c9c931d477de840d160a30b032226c25b282"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-003-resident-session"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-002"]
acceptance = "Make resident model/tokenizer reuse the normal multi-prompt path, with session-owned buffers, safe runtime-state reuse, per-prompt receipts, and an aggregate receipt separating model_load, tokenize, prefill, decode, sampling, and total timing."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set operator --json-out target/apple-m4-slm-performance/M4-SLM-PERF-003/resident-session.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check target/apple-m4-slm-performance/M4-SLM-PERF-003/resident-session.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Apple M4 SLM warm-session model and tokenizer reuse are hardened for the normal multi-prompt path.",
  "Per-prompt and aggregate receipts record resident-session timing and reuse state.",
]
must_not_claim = [
  "CPU/NEON performance is improved.",
  "A measured speed envelope is published.",
  "Full apple-m4-metal inference works.",
  "BitNet local-answer quality or performance is proven.",
]

[[work_item]]
id = "M4-SLM-PERF-004"
status = "merged"
pr = 4057
merge_sha = "61953abd85ba4ecf1ba4025831e93974b2fde639"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-004-cpu-neon-hotpath"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-003"]
acceptance = "Optimize the highest measured Apple M4 CPU/NEON bottleneck while preserving greedy token IDs, deterministic quality corpus results, fallback status, and before/after warm-session receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set performance --json-out target/apple-m4-slm-performance/M4-SLM-PERF-004/cpu-neon-hotpath.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check target/apple-m4-slm-performance/M4-SLM-PERF-004/cpu-neon-hotpath.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "A measured Apple M4 CPU/NEON bottleneck is optimized with before/after receipts.",
  "Greedy output identity and quality gates are preserved for the optimized path.",
]
must_not_claim = [
  "Broad M4 performance is proven.",
  "Full apple-m4-metal inference works.",
  "BitNet local-answer quality or performance is proven.",
  "A Metal phase is optimized.",
]

[[work_item]]
id = "M4-SLM-PERF-005"
status = "merged"
pr = 4069
merge_sha = "731f399a6147b1981fa6bffb4a90b29aabbe4ce1"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-005-metal-phase-expansion"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-004"]
acceptance = "Expand only named Apple Metal prefill/projection phases after CPU baseline stability, requiring CPU-only versus CPU-plus-Metal greedy parity, Metal phase fallback_used=false, explicit CPU fallback for the rest of the pipeline, and timing delta receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,metal,full-cli",
  "cargo test --locked -p bitnet-kernels --no-default-features --features metal",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-kernels/tests/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
  "ci/hardware/apple-m4-mac-mini/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "A named Apple Metal prefill/projection phase participates in the SLM path with CPU-only versus CPU-plus-Metal greedy parity.",
  "The named Metal phase records fallback_used=false and timing delta receipts for that phase only.",
]
must_not_claim = [
  "Full apple-m4-metal inference works.",
  "Apple Metal accelerates the full SLM answer path.",
  "BitNet local-answer quality or performance is proven.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
  "Broad Apple M4 performance is proven.",
]

[[work_item]]
id = "M4-SLM-PERF-006"
status = "merged"
pr = 4075
merge_sha = "549d91ed8c9d431659b7a131dbb284e668ee243d"
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-006-streaming-ux"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-005"]
acceptance = "Add streaming token output, time-to-first-token receipts, quiet default logs, operator-friendly progress, and clear failure messages without changing backend claim boundaries."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/**/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Apple M4 SLM warm-session runs can stream generated token text on request and record time-to-first-token UX fields in receipts.",
  "Mac validation runs can emit operator progress on request while keeping default receipt-oriented runs quiet.",
]
must_not_claim = [
  "Streaming improves model speed or output quality.",
  "Broad Apple M4 performance is proven.",
  "Full apple-m4-metal inference works.",
  "BitNet local-answer quality or performance is proven.",
  "QK256 works on Apple Silicon.",
  "Neural Engine execution is proven.",
]

[[work_item]]
id = "M4-SLM-PERF-007"
status = "pr_open"
pr = 4081
branch = "codex/apple-m4-slm-performance/M4-SLM-PERF-007-performance-envelope"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SLM-PERF-006"]
acceptance = "Publish a measured Apple M4 SLM performance envelope for supported models and profiles only, recording machine context, backend, profile, timings, phase contributions, fallback status, and explicit unsupported claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-slm-performance",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-slm-performance/**",
  "docs/tracking/generated/**",
  "ci/hardware/apple-m4-mac-mini/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "A measured Apple M4 SLM performance envelope exists for the named supported model, profiles, backend, and machine receipts.",
  "Cold load, warm answer timing, and named Metal phase contributions are reported only for the measured profiles.",
]
must_not_claim = [
  "The measured envelope proves broad Apple M4 performance.",
  "The measured envelope proves full apple-m4-metal inference.",
  "The measured envelope proves BitNet local-answer quality or performance.",
  "The measured envelope proves QK256 works on Apple Silicon.",
  "The measured envelope proves Neural Engine execution.",
]
