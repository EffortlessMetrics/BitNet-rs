timestamp = "2026-05-12T13:24:22Z"
campaign = "slm-cpu"
item = "SLM-CPU-008R"
event = "in_progress"
actor = "codex"

notes = [
  "After #4434 merged SLM-CPU-008 without a fresh real-model i5-8250U run in that worktree, the next bounded item is post-008 artifact revalidation on the verified Qwen3-0.6B Q8_0 GGUF.",
  "The item must record strict loader/tokenizer provenance, prompt IDs, generated IDs, first-step top-k, selected CPU backend, fallback=false, and reference comparison without claiming answer quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, quant expansion, or BitNet QK256 changes.",
]
