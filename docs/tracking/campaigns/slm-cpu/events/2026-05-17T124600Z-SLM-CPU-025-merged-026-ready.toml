schema_version = "1.0"
campaign = "slm-cpu"
item = "SLM-CPU-025"
event = "merged"
timestamp = "2026-05-17T12:46:00Z"
actor = "codex"
pr = 5357
merge_sha = "1123eafcf28968a43c9b293994a9ca728ae44ac8"
branch = "codex/slm-cpu-025-logits-extraction-reuse"

summary = "Merged #5357 for Qwen3 Q8_0 warm-session logits extraction isolation and queued SLM-CPU-026 for the remaining repetition-penalty logits Vec path."

artifacts = [
  "https://github.com/EffortlessMetrics/BitNet-rs/pull/5357",
  "ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-logits-extraction-reuse.json",
  "ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-logits-extraction-reuse-validation.json",
  "docs/slm/SLM_CPU_KABY_PERFORMANCE_DASHBOARD.md",
]

notes = [
  "The SLM-CPU-025 after artifact preserves generated IDs, decoded text, model SHA, GGUF tokenizer authority, selected CPU backend, and fallback=false against the SLM-CPU-024 baseline.",
  "The direct tensor argmax bypass covers exact deterministic greedy fast-path steps.",
  "The remaining default repetition-penalty logits Vec extraction steps are explicit and are queued as SLM-CPU-026 rather than silently claimed removed.",
]

validation = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli direct_greedy_logits_guard_matches_sampler_fast_path_policy",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli greedy_argmax_token_2d_matches_lowest_id_tie_policy",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-logits-extraction-reuse.json",
  "python -m json.tool ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-logits-extraction-reuse-validation.json",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
