schema_version = "1.0"
campaign = "slm-cpu"
item = "SLM-CPU-028"
event = "pr_open"
timestamp = "2026-05-17T16:05:00Z"
actor = "codex"
pr = 5384
head_sha = "ee672a2f72f1de3a4f655741b3580dfba4fcfc7f"
branch = "codex/slm-cpu-028-q4-planning-gates"

summary = "Opened #5384 with bounded Q4_K_M/Q4_K_S expansion gates for the Kaby Lake SLM lane."

artifacts = [
  "https://github.com/EffortlessMetrics/BitNet-rs/pull/5384",
  "docs/slm/SLM_CPU_Q4_EXPANSION_PLAN.md",
  "ci/slm-cpu/q4-expansion-candidates.toml",
]

notes = [
  "The plan keeps Qwen3 Q8_0 as the Kaby appliance behavior oracle and requires strict generated-ID and provenance preservation before Q4 support can be claimed.",
  "The candidate manifest pins Qwen2.5-0.5B-Instruct Q4_K_M as the first official Q4_K_M sanity target and records a community Qwen3 Q4_K_M comparison candidate separately.",
  "Q4_K_S remains fail-closed until an exact artifact, SHA256, metadata, tokenizer authority, and tensor policy are pinned.",
  "No Q4 runtime support, sustained throughput, broad answer quality, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 claim is made.",
]

validation = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
