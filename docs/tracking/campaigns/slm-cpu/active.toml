id = "slm-cpu"
title = "Small dense model CPU proof"
status = "active"

objective = "Make the Intel i5-8250U a strict CPU proof host for small dense transformer GGUF models by proving metadata, tokenizer, architecture adapter, CPU execution, and tiny answer evidence without claiming throughput."

end_state = [
  "A dense GGUF candidate is selected by verified architecture and tokenizer metadata.",
  "Tokenizer source and strictness are recorded.",
  "Dense architecture metadata maps into a typed adapter contract.",
  "Tiny CPU answer receipts record prompt IDs, generated IDs, decoded text, selected backend/kernel, and fallback=false.",
  "8250U evidence is correctness and operability evidence, not performance evidence.",
]

hard_constraints = [
  "Do not edit BitNet QK256/I2_S kernels.",
  "Do not change BitNet proof receipt semantics.",
  "Do not claim sustained 8250U throughput.",
  "Do not claim server, GPU, OpenVINO, UHD 620, or NPU execution.",
  "Do not use model name recognition as proof of GGUF compatibility.",
]

[[work_item]]
id = "SLM-CPU-000"
status = "merged"
pr = 3902
branch = "codex/slm-cpu-000-8250u-lane"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Define a separate 8250U dense SLM CPU proof lane with explicit target policy, dense GGUF requirements, tokenizer authority rules, architecture adapter boundaries, receipt fields, and claim boundaries. The current first candidate policy prefers official Qwen3-0.6B-GGUF Q8_0 after exact artifact metadata verification."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The 8250U dense SLM CPU lane contract exists after merge.",
  "Qwen3-0.6B-GGUF is the preferred first candidate class, pending exact GGUF metadata verification.",
]
must_not_claim = [
  "A dense GGUF artifact is verified.",
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-001"
status = "merged"
pr = 3905
branch = "codex/slm-cpu-001-model-manifest"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-000"]
acceptance = "Add a model candidate manifest, artifact policy, and 8250U runbook. The target policy was updated by SLM-CPU-002 to prefer official Qwen3-0.6B-GGUF Q8_0, with Qwen2.5-0.5B-Instruct as a conservative fallback only after exact artifact metadata is verified."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Dense SLM model candidate policy and 8250U runbook exist after merge.",
]
must_not_claim = [
  "The first GGUF artifact is verified.",
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002"
status = "merged"
pr = 3910
branch = "codex/slm-cpu-002-metadata-preflight"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-001"]
acceptance = "Add strict SLM CPU metadata preflight that verifies model path, pinned SHA256, GGUF architecture, tokenizer source, context cap, quant format, adapter selection, and fallback=false without running inference."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "docs/slm/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Dense SLM metadata preflight exists after merge.",
]
must_not_claim = [
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002A"
status = "merged"
pr = 3917
merge_sha = "a2b9b468a266683279e16bee2f8d12ab03cf4350"
branch = "codex/slm-cpu-002a-qwen-strict-blocker"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002"]
acceptance = "Recognize qwen2/qwen3 in the authoritative GGUF loader metadata path and make strict dense Qwen Q8_0/Q*_K GGUF attempts fail at an explicit unsupported-quantization boundary before compatibility fallback or misleading tensor validation."
commands = [
  "cargo check --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features dense_qwen --lib",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The official Qwen3-0.6B Q8_0 artifact reaches an explicit dense-quantization blocker in strict mode.",
]
must_not_claim = [
  "Dense CPU inference works.",
  "Q8_0 dense kernels or dequantization are implemented.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002B"
status = "merged"
pr = 3926
merge_sha = "726e2efd8fb0224d740f608ef5e4079475653ae4"
branch = "codex/slm-cpu-002b-dense-q8-gguf"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002A"]
acceptance = "Add dense standard GGUF Q8_0/Q*_K adapter or dequantization support sufficient for the verified Qwen3-0.6B Q8_0 artifact to pass strict load without compatibility fallback, while preserving explicit failure for unsupported dense quantization variants."
commands = [
  "cargo check --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features dense_qwen --lib",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/i2s_qk256*",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3-0.6B Q8_0 can pass strict dense GGUF load after merge.",
]
must_not_claim = [
  "Tiny dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-003"
status = "merged"
pr = 3940
merge_sha = "566472d3be1965d958154b2c2f5cc364ef25e52d"
branch = "codex/slm-cpu-003-tiny-strict-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002B"]
acceptance = "Run one tiny strict dense CPU receipt on the verified candidate with real GGUF, strict tokenizer, dense architecture adapter, CPU backend, fallback=false, prompt IDs, generated IDs, and decoded text. Correct answer is not required for bring-up if the receipt is diagnosable."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "xtask/src/campaign.rs",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "A tiny dense SLM CPU run receipt exists after merge.",
]
must_not_claim = [
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-004"
status = "merged"
pr = 3957
merge_sha = "a9e7835e6480b9277b80bf76cda6be6653942eec"
branch = "codex/slm-cpu-004-answer-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-003"]
acceptance = "Add a tiny SLM answer corpus with receipt rows recording model SHA, architecture, tokenizer source, prompt IDs, generated IDs, decoded text, selected backend, fallback status, quality pass/fail, and failed rules."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Tiny SLM answer corpus evidence exists after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-005"
status = "merged"
pr = 3969
merge_sha = "ae2fc47e742100c47c40b0723e234b3191b115d1"
branch = "codex/slm-cpu-005-reference-divergence"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-004"]
acceptance = "Add a machine-checkable reference divergence artifact schema and validator comparing bitnet-rs against a known-good external run by model SHA, prompt/template/BOS policy, prompt IDs, generated IDs, decoded text, top-k when available, and first divergence."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM reference divergence artifacts can be validated after merge.",
]
must_not_claim = [
  "The repo can run the external reference engine.",
  "General chat quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-006A"
status = "merged"
pr = 4051
merge_sha = "9974bbcc66ad6abd5c7867a4193744186283bc2a"
branch = "codex/slm-cpu-006-first-token-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-005"]
acceptance = "Land first-token divergence support tooling for the Qwen3 i5-8250U lane: --logits-dump-steps alias support, reference-compare first-token failure classification, and documented first-token workflow. This support slice does not capture the actual bitnet-rs and external-reference artifact pair."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The first-token divergence tooling and runbook support landed in #4051.",
  "The actual Qwen3 first-token artifact capture remains open as SLM-CPU-006B.",
]
must_not_claim = [
  "The actual i5-8250U Qwen3 first-token artifact pair is captured.",
  "Qwen answer quality is proven.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-006B"
status = "merged"
pr = 4096
merge_sha = "ee8e7ec0f4c0a7ed5cee1a10729177d5f823d02d"
branch = "codex/slm-cpu-006b-first-token-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-006A"]
acceptance = "Capture a real first-token divergence artifact for Qwen3-0.6B Q8_0 on the i5-8250U by running bitnet-rs and a known-good external reference with identical model SHA, prompt text, Qwen template, BOS policy, prompt IDs, generated IDs, decoded text, chosen token, and first-step top-k/logit evidence where available. The artifact classifies the first divergence as prompt/tokenizer/template, logits/sampler, output-head/vocab indexing, or shared transformer math without claiming answer quality, throughput, server, GPU, NPU, or Qwen3.5 support."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3 first-token divergence can be localized against reference prompt/token/logit evidence after merge.",
  "The SLM-CPU lane remains a correctness and operability lane for the i5-8250U, not a throughput lane.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-007A"
status = "merged"
pr = 4132
merge_sha = "afb6a84bd66ea638b8122240869ee35a0eb4032e"
branch = "codex/slm-cpu-007-checkpoint-probes"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-006B"]
acceptance = "Expose the bounded Qwen3 first-token checkpoint trace surface through bitnet run flags for JSONL output, trace layer selection, full-prompt trace, and prompt-ID override, and document the first-drift workflow. This is a support slice only and does not claim first-drift localization, first-token parity, answer quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli run_help_documents_qwen_trace_flags",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The bitnet run command exposes bounded Qwen3 checkpoint trace controls after merge.",
  "The first-drift workflow is documented for dense Qwen3 correctness triage after merge.",
]
must_not_claim = [
  "The Qwen3 first-token logits divergence is localized.",
  "Qwen answer quality is proven.",
  "Qwen first-token parity is restored.",
  "Qwen3 tiny answer corpus is green.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-007B"
status = "merged"
pr = 4138
merge_sha = "fcc0f992a00e39fb97bb5d0770a9b1b2295322ad"
branch = "codex/slm-cpu-007b-first-drift-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-007A"]
acceptance = "Capture bitnet-rs Qwen3 checkpoint JSONL plus known-good reference prompt/top-logit evidence for the same Qwen3-0.6B Q8_0 model SHA, prompt/template/BOS policy, prompt IDs, greedy settings, and selected CPU backend as SLM-CPU-006B; validate and classify the first comparable divergence as logits and the first internal drift as reference-missing until reference internal checkpoint dumps exist, without claiming answer quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The Qwen3 first comparable divergence is localized to first-token top logits after merge.",
  "Bounded bitnet-rs checkpoint evidence exists for dense Qwen3 correctness triage after merge.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen first-token parity is restored.",
  "Qwen3 tiny answer corpus is green.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008"
status = "merged"
pr = 4434
branch = "codex/slm-cpu-008-qwen3-first-token-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-007B"]
acceptance = "Fix the first Qwen3-0.6B Q8_0 divergent operation using the SLM-CPU-007B checkpoint evidence and any required known-good reference checkpoints so bitnet-rs matches the reference first generated token for the same model SHA, prompt/template/BOS policy, prompt IDs, greedy settings, and fallback=false. The target reference token is 19 / '4'. No answer-quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, quant-expansion, or BitNet QK256 claim is allowed."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3 first-token parity for the fixed prompt exists after merge.",
  "The fix is bounded to dense Qwen3 CPU correctness.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008R"
status = "merged"
pr = 4572
merge_sha = "5414b66dc07921e2d9c4eafa6f9140d2e6b8eef6"
branch = "codex/slm-cpu-008r-post008-artifact-revalidation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008"]
acceptance = "Run the verified Qwen3-0.6B Q8_0 GGUF on the actual i5-8250U after #4434 and record whether first-token parity is actually achieved with real GGUF, strict tokenizer from GGUF metadata, selected_backend=cpu-rust, fallback_used=false, rendered prompt, prompt IDs, generated IDs, first-step top-k, and reference comparison. This is an artifact revalidation item only and must not claim answer quality, sustained throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, quant expansion, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Post-#4434 first-token parity has been revalidated on the actual i5-8250U artifact after merge.",
  "The post-#4434 artifact records strict loader/tokenizer provenance, prompt IDs, generated IDs, top-k, selected CPU backend, and fallback=false.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008S"
status = "merged"
pr = 4606
merge_sha = "5dfec8ceb794f01aded1939ef72b98aac50c224a"
branch = "codex/slm-cpu-008s-qwen3-output-head-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008R"]
acceptance = "Use the SLM-CPU-008R post-#4434 artifact and trace to localize the remaining Qwen3-0.6B Q8_0 first-token divergence, prioritizing output head/vocab indexing and shared transformer math. The item must identify the next concrete drift point or add the smallest missing diagnostic needed to do so, while preserving strict loader/tokenizer provenance and fallback=false. No answer-quality, tiny corpus, sustained throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, Q4/Q5 quant expansion, or BitNet QK256 claim is allowed."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features \"cpu,full-cli\" reference_compare",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-common/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The post-#4434 Qwen3 first-token divergence is being localized from actual i5-8250U evidence.",
  "The next drift domain is output-head/vocab-indexing-or-shared-transformer-math unless new diagnostics prove otherwise.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008T"
status = "merged"
pr = 4611
merge_sha = "35be945a7ddaf9f593d8d405a961e08c553fe7fc"
branch = "codex/slm-cpu-008t-first-token-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008S"]
acceptance = "Fix the post-008 Qwen3-0.6B Q8_0 first-token parity candidate by selecting the dedicated GGUF output.weight head before tied-embedding fallback, then require a real i5-8250U artifact refresh to prove whether bitnet-rs now matches the reference token 19 / '4' for the same model SHA, rendered prompt, prompt IDs, greedy settings, strict tokenizer, selected CPU backend, and fallback=false. This slice must not claim answer quality, tiny corpus, multi-token stability, warm-session performance, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_output_weight_when_present",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_lm_head_when_present",
  "cargo check --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The transformer now selects a dedicated GGUF output.weight head before tied-embedding fallback.",
  "The post-008 first-token parity candidate is ready for real i5-8250U artifact refresh after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored before the real artifact refresh proves it.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008U"
status = "merged"
pr = 4617
merge_sha = "edac33c25311d457ed6bfacb1642daaf76b2f8ae"
branch = "codex/slm-cpu-008u-output-weight-layout"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008T"]
acceptance = "Fix the remaining Qwen3 output-head layout candidate by treating GGUF output.weight with hidden/vocab dimensions as token-major storage that must be reshaped to vocab/hidden without transposing values, while preserving true transposed lm_head.weight handling. This slice must not claim Qwen3 first-token parity until a real i5-8250U artifact refresh proves bitnet-rs matches the reference token 19 / '4' with strict loader/tokenizer provenance and fallback=false."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_reshapes_gguf_output_weight_hidden_vocab_without_transpose",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_keeps_hidden_vocab_lm_head_transposed",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_output_weight_when_present",
  "cargo check --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "GGUF output.weight hidden/vocab dims are handled as token-major storage instead of a transposed lm head after merge.",
  "The post-008 first-token parity candidate is ready for real i5-8250U artifact refresh after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored before the real artifact refresh proves it.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008V"
status = "merged"
pr = 4633
merge_sha = "45db888b376ffd85a3be3cdc56af2fb3cbb0a8b0"
branch = "codex/slm-cpu-008v-post-008u-artifact-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008U"]
acceptance = "Refresh the real i5-8250U Qwen3-0.6B Q8_0 first-token artifact after SLM-CPU-008U, validate it against the known-good reference token 19 / '4', and audit the output-head/tied-head boundary. The artifact must record real GGUF, strict GGUF tokenizer authority, prompt IDs, generated IDs, first-step top-k, selected CPU backend, fallback=false, and whether first-token parity is restored. This slice must not claim answer quality, tiny corpus, multi-token stability, warm-session performance, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "Get-FileHash C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf -Algorithm SHA256",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu run --model C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf --prompt-template qwen --prompt \"What is 2+2? Answer with only the number.\" --max-new-tokens 1 --temperature 0.0 --greedy --deterministic --strict-loader --strict-tokenizer --logits-dump-steps 1 --logits-topk 10 --assert-greedy --qwen-trace-jsonl ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-trace.jsonl --qwen-trace-layer 0 --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-first-token.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- output-head-logits-audit --machine-id intel-i5-8250u-cpu-avx2 --model C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf --avx2-answer-corpus ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-first-token.json --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-output-head-audit.json",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli output_head",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-reference-compare.json --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-reference-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/commands/output_head_logits_audit.rs",
  "crates/bitnet-cli/tests/cli_arg_tests.rs",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Post-SLM-CPU-008U first-token parity was revalidated on the real i5-8250U artifact.",
  "The official Qwen3-0.6B Q8_0 GGUF output-head/tied-head boundary was audited from GGUF metadata.",
  "The next root-cause domain is tied-token-embedding logits or shared transformer math if parity still fails.",
]
must_not_claim = [
  "Qwen first-token parity is restored unless the artifact emits the reference token 19 / 4.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008W"
status = "merged"
pr = 4641
merge_sha = "08530ef97765b99bd89d2526c5dfd1714caa0583"
branch = "codex/slm-cpu-008w-tied-head-shared-math-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008V"]
acceptance = "Use the post-SLM-CPU-008U artifact refresh and output-head audit to localize the remaining first-token divergence after the official Qwen3 GGUF is proven to use tied token embeddings rather than a dedicated output.weight head. Prioritize tied embedding logits, final norm input, vocab-index handoff, and shared transformer math; only move backward into RoPE, GQA, MLP ordering, tensor orientation, or Q8_0 dequant if the new evidence points there. This item must not claim answer quality, corpus success, performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The next Qwen3 first-token drift point is narrowed beyond the dedicated output.weight hypothesis.",
]
must_not_claim = [
  "Qwen first-token parity is restored before a real artifact proves token 19 / 4.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]
