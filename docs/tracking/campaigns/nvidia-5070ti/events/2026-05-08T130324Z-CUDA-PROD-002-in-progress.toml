timestamp = "2026-05-08T13:03:24Z"
campaign = "nvidia-5070ti"
item = "CUDA-PROD-002"
event = "in_progress"
actor = "codex"
branch = "codex/cuda-prod/CUDA-PROD-002-warm-session-receipts"
previous_status = "ready"
new_status = "in_progress"

notes = [
  "Started strict RTX 5070 Ti CUDA warm-session receipts after CUDA-PROD-001 merged.",
  "The scope is a multi-prompt session path that loads the model and tokenizer once, reuses the QK256 CUDA context and upload-once weight handles, and writes per-turn plus aggregate receipts.",
  "No kernel, transformer math, tokenizer, loader, server, speed, broad chat-quality, or full-residency claim is in scope.",
]
