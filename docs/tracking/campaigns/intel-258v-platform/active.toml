id = "intel-258v-platform"
title = "Intel 258V platform validation"
status = "active"

objective = "Validate Core Ultra 7 258V as the BitNet CPU lead and tri-device platform while keeping CPU AVX2, Arc 140V GPU, and Intel AI Boost NPU proof labels separate."

end_state = [
  "Same-machine CPU, GPU, and NPU facts are captured.",
  "258V CPU strict real-GGUF validation, scalar/AVX2 answer parity, and phase receipts provide the CPU reference plate.",
  "Arc 140V OpenCL, OpenVINO GPU, and OpenVINO NPU evidence are not conflated.",
  "Receipts record OS, drivers, memory, power, thermal, and WSL/native visibility context.",
]

hard_constraints = [
  "258V CPU proof is first priority; NPU and Arc proofs must compare against the 258V CPU reference before BitNet-adjacent parity claims.",
  "Arc 140V OpenCL proof is not NPU proof.",
  "OpenVINO GPU smoke is not packed BitNet kernel proof.",
  "WSL only counts for NPU validation if OpenVINO reports NPU inside WSL.",
]

[[work_item]]
id = "LNL258V-RUN-001"
status = "merged"
branch = "codex/intel-258v/LNL258V-RUN-001-platform-probe"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a JSON-ready Lunar Lake 258V platform probe that records CPU AVX2 facts, Arc 140V OpenCL/Level Zero/OpenVINO GPU visibility, Intel NPU OS/OpenVINO visibility, memory, power, OS, proof_stage=runtime_detected, and fallback_used=false without inference claims."
commands = [
  "cargo test --locked -p bitnet-device-probe --no-default-features",
  "cargo test --locked -p bitnet-device-probe --no-default-features --features cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/src/intel/lunar_lake/**",
  "crates/bitnet-device-probe/src/intel/mod.rs",
  "crates/bitnet-device-probe/src/cpu/**",
  "crates/bitnet-device-probe/src/runtimes/**",
  "crates/bitnet-device-probe/src/lib.rs",
  "crates/bitnet-device-probe/tests/**",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Lunar Lake runtime visibility can be recorded after merge.",
]
must_not_claim = [
  "BitNet inference works on 258V.",
  "Arc 140V execution works.",
  "Intel NPU execution works.",
  "NPU accelerates BitNet.",
]

[[work_item]]
id = "ARC140V-002"
status = "merged"
branch = "codex/intel-arc/ARC140V-002-runtime-probe"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["LNL258V-RUN-001"]
acceptance = "Probe exact Arc 140V runtime visibility by name or PCI ID 0x64A0 across OpenCL, Level Zero, and OpenVINO GPU.0 while recording proof_stage=runtime_detected, requested/selected backend identity, runtime API, and fallback_used=false."
commands = [
  "cargo test --locked -p bitnet-device-probe --no-default-features lunar_lake -- --nocapture",
  "cargo test --locked -p bitnet-device-probe --no-default-features level_zero -- --nocapture",
  "cargo test --locked -p bitnet-device-probe --no-default-features --features cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/src/intel/lunar_lake/arc140v.rs",
  "crates/bitnet-device-probe/src/runtimes/opencl.rs",
  "crates/bitnet-device-probe/src/runtimes/level_zero.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/tests/**",
  "docs/specs/intel-lunar-lake-gpu-roadmap.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Arc 140V runtime visibility is recorded when OpenCL, Level Zero, or OpenVINO GPU.0 exact identity evidence is present.",
]
must_not_claim = [
  "Arc 140V OpenCL execution works.",
  "OpenVINO GPU.0 smoke proves native OpenCL kernels.",
  "BitNet inference works on Arc 140V.",
]

[[work_item]]
id = "ARC140V-003"
status = "merged"
pr = 3942
branch = "codex/intel-258v-platform/ARC140V-003-openvino-gpu-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["ARC140V-002", "CPU258V-003"]
acceptance = "Run a tiny static OpenVINO GPU.0 graph smoke for the Arc 140V reference lane, recording Arc 140V device identity, graph shape/timing/tolerance fields, fallback=false, and no BitNet/QK256/native OpenCL claims."
commands = [
  "cargo test --locked -p bitnet-device-probe --no-default-features openvino_gpu -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet intel_arc::tests -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- intel-arc-140v-openvino-gpu-smoke --json-out target/arc140v-openvino-gpu-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "crates/bitnet-cli/src/intel_arc.rs",
  "crates/bitnet-cli/src/main.rs",
  "docs/specs/intel-lunar-lake-gpu-roadmap.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Arc 140V OpenVINO GPU.0 tiny graph smoke is available when GPU.0 resolves to Arc 140V and the static graph output matches CPU expected output.",
]
must_not_claim = [
  "Arc 140V native OpenCL execution works.",
  "BitNet inference works on Arc 140V.",
  "Packed QK256 decode works on Arc 140V.",
  "OpenVINO GPU smoke proves acceleration or native BitNet kernels.",
  "CPU fallback satisfies Arc 140V proof.",
]

[[work_item]]
id = "ARC140V-004"
status = "merged"
pr = 3953
branch = "codex/intel-258v-platform/ARC140V-004-opencl-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["ARC140V-003", "CPU258V-003"]
acceptance = "Run a tiny native OpenCL vector-add kernel on Arc 140V, record runtime/device identity, kernel shape/timing/tolerance fields, fallback=false, and no BitNet/QK256/OpenVINO acceleration claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_arc.rs crates/bitnet-device-probe/src/opencl.rs crates/bitnet-device-probe/src/runtimes/opencl.rs crates/bitnet-device-probe/src/runtimes/mod.rs --edition 2024",
  "cargo test --locked -p bitnet-device-probe --no-default-features runtimes::opencl::tests -- --nocapture",
  "cargo test --locked -p bitnet-device-probe --no-default-features --features opencl opencl -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet intel_arc::tests::intel_arc_opencl_smoke_receipt_records_kernel_execution_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet intel_arc::tests::strict_intel_arc_opencl_smoke_records_error_without_fallback -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli,opencl -- intel-arc-140v-opencl-smoke --json-out ci/hardware/intel-258v/2026-05-07/arc-140v-opencl-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/src/opencl.rs",
  "crates/bitnet-device-probe/src/runtimes/opencl.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "crates/bitnet-cli/Cargo.toml",
  "crates/bitnet-cli/src/intel_arc.rs",
  "crates/bitnet-cli/src/main.rs",
  "ci/hardware/intel-258v/**",
  "docs/specs/intel-lunar-lake-gpu-roadmap.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Arc 140V native OpenCL can execute a tiny vector-add kernel when the selected OpenCL device resolves to Arc 140V and output matches CPU expected output.",
]
must_not_claim = [
  "BitNet inference works on Arc 140V.",
  "Packed QK256 decode works on Arc 140V.",
  "OpenVINO GPU smoke proves native OpenCL execution.",
  "Arc 140V accelerates BitNet.",
  "CPU fallback satisfies Arc 140V proof.",
]

[[work_item]]
id = "LNL258V-002"
status = "merged"
branch = "codex/intel-258v-platform/LNL258V-002-probe-bundle"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a 258V platform probe bundle that records CPU, Arc 140V GPU, Intel NPU, memory, OS, driver, OpenVINO, OpenCL, Level Zero, WSL, and power context without runtime claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-platform-roadmap.md",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/specs/intel-lunar-lake-gpu-roadmap.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "258V platform probe fields and artifact paths are documented after merge.",
]
must_not_claim = [
  "Arc 140V execution works.",
  "Intel NPU execution works.",
  "BitNet inference works on 258V.",
]

[[work_item]]
id = "LNL258V-003"
status = "merged"
branch = "codex/intel-258v-platform/LNL258V-003-cli-platform-probe"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["LNL258V-002"]
acceptance = "Add a CLI command that emits the Lunar Lake 258V visibility-only platform probe receipt from the current machine without launching kernels, compiling OpenVINO graphs, loading BitNet models, or making execution claims."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --bin bitnet tests::lunar_lake_probe_receipt_is_visibility_only -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- lunar-lake-probe --json-out target/lnl258v-platform-probe.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
  "tests/**",
]
may_claim = [
  "The CLI can emit a Lunar Lake runtime visibility receipt.",
]
must_not_claim = [
  "BitNet inference works on 258V.",
  "Arc 140V execution works.",
    "Intel NPU execution works.",
    "OpenVINO graph execution works.",
]

[[work_item]]
id = "CPU258V-001"
status = "merged"
branch = "codex/intel-258v-platform/CPU258V-001-validation-harness"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["LNL258V-003"]
acceptance = "Add a validation-only Core Ultra 7 258V CPU BitNet preflight command that emits structured blocked_preflight or preflight_ready artifacts without changing GGUF loader, tokenizer, QK256 layout, QK256 dispatch, CPU kernels, or transformer decode internals."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --bin bitnet tests::cpu258v_validation_blocks_missing_model_before_inference -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet tests::cpu258v_validation_rejects_accelerator_backend -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- validate cpu-bitnet --machine intel-258v --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/BitNet-b1.58-2B-4T/tokenizer.json --backend cpu --strict --max-tokens 1 --platform-artifact ci/hardware/intel-258v/2026-05-06/platform-probe.json --json-out target/cpu258v-bitnet-validation.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The 258V CPU validation harness can emit blocked_preflight or preflight_ready artifacts.",
]
must_not_claim = [
  "Strict BitNet GGUF loaded on 258V.",
  "QK256 or TL2 execution ran on 258V.",
  "BitNet inference works on 258V.",
  "258V CPU benchmark performance.",
]

[[work_item]]
id = "LNL258V-OWNERSHIP-001"
status = "merged"
pr = 3914
branch = "codex/docs/lnl-bitnet-cpu-lead"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-001"]
acceptance = "Update Lunar Lake ownership docs and trackers so the 258V CPU is the BitNet CPU lead, 8250U is the SLM CPU lead plus legacy/low-power BitNet comparison lane, 9950X3D and 5700X are support validators, and the priority order is 258V CPU first, Intel NPU second, Arc 140V third."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".codex/bitnet-alignment.md",
  "docs/hardware/LANE_OWNERSHIP.md",
  "docs/hardware/HARDWARE_MATRIX.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
  "ci/hardware/**",
]
may_claim = [
  "The ownership/control-plane docs route new BitNet CPU proof through the 258V CPU after merge.",
  "CPU258V-002 and CPU258V-003 are discoverable as the next Lunar Lake CPU proof items after merge.",
]
must_not_claim = [
  "New runtime proof was added.",
  "258V CPU parity or phase benchmarks ran.",
  "Intel NPU or Arc 140V execution changed.",
]

[[work_item]]
id = "CPU258V-002"
status = "merged"
pr = 3929
branch = "codex/intel-258v-platform/CPU258V-002-answer-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Run scalar-vs-AVX2 strict CPU answer parity on the 258V using the same GGUF, tokenizer, prompt, greedy settings, prompt token IDs, generated token IDs/text, first-divergence evidence, logits/top-k evidence when available, fallback=false, selected kernels, and P-core/LP-E/power context."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet commands::answer_parity::tests -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet commands::answer_corpus::tests -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-parity --scalar ci/hardware/intel-258v/<date>/cpu-answer-corpus-scalar.json --avx2 ci/hardware/intel-258v/<date>/cpu-answer-corpus-avx2.json --json-out ci/hardware/intel-258v/<date>/cpu-answer-parity.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "258V scalar-vs-AVX2 strict CPU answer parity can be audited after merge.",
  "First-divergence evidence can separate AVX2-specific divergence from shared 258V decode, prompt, tokenizer, logits, or sampler issues after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "258V sustained throughput is proven.",
  "Arc 140V execution is involved.",
  "Intel NPU execution is involved.",
]

[[work_item]]
id = "CPU258V-003"
status = "merged"
pr = 3938
branch = "codex/intel-258v-platform/CPU258V-003-phase-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add 258V phase benchmark receipts for smoke_1, first_token, decode_128, and prefill_512 when available, recording selected backend/kernel, fallback status, CPU feature set, P-core/LP-E topology, power mode, memory context, and explicit not_run gaps for unavailable profiles."
commands = [
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "Parse ci/hardware/intel-258v/<date>/cpu-phase-benchmark.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-258v-buildout-plan.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "258V phase timing receipt fields are available for CPU reference comparison after merge.",
]
must_not_claim = [
  "Sustained 258V throughput is proven unless a sustained receipt exists.",
  "Arc 140V or Intel NPU performance is proven.",
  "Full platform performance comparison is complete.",
]

[[work_item]]
id = "CPU258V-004"
status = "merged"
pr = 3981
branch = "codex/intel-258v-platform/CPU258V-004-phase-thresholds"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-003"]
acceptance = "Require 258V CPU phase benchmark receipts to keep decode_128 and prefill_512 as not_run unless supplied strict CPU proof receipts meet the corresponding generated-token and prompt-token thresholds, preserving explicit gaps rather than promoting proxy evidence."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-bench-receipts/src/bin/cpu_phase_benchmark_receipt.rs --edition 2024",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features cpu258v_003_ -- --nocapture",
  "cargo run --locked -p bitnet-bench-receipts --bin cpu_phase_benchmark_receipt --no-default-features -- --strict-proof-receipt ci/hardware/intel-258v/2026-05-06/strict-bitnet-cpu-proof.json --machine-id intel-258v --hardware-lane intel-258v-cpu-avx2 --selected-backend cpu-rust --model-quant-format QK256/I2_S --platform-artifact ci/hardware/intel-258v/2026-05-06/platform-probe.json --receipt-out target/cpu258v-phase-threshold-check.json",
  "Parse target/cpu258v-phase-threshold-check.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/src/bin/cpu_phase_benchmark_receipt.rs",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "258V phase receipts preserve not_run gaps when strict proof receipts do not satisfy decode_128 or prefill_512 token thresholds.",
]
must_not_claim = [
  "decode_128 is measured from a short decode proof.",
  "prefill_512 is measured from a short prompt proof.",
  "Sustained 258V throughput is proven.",
  "Arc 140V or Intel NPU performance is proven.",
]

[[work_item]]
id = "CPU258V-005"
status = "merged"
pr = 3999
branch = "codex/intel-258v-platform/CPU258V-005-phase-evidence"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-004"]
acceptance = "Record local strict CPU attempts to collect 258V prefill_512 and decode_128 phase evidence, preserve missing proof receipts as blocker evidence, and require a receipt-emitting warm phase runner or faster decode path before promoting these profiles."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-phase-evidence-attempts.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "258V strict CPU phase evidence collection attempted calibrated prefill_512 prompts and emitted no proof receipt within the local run windows.",
  "decode_128 and prefill_512 remain unmeasured gaps for the 258V CPU lane.",
]
must_not_claim = [
  "prefill_512 is measured.",
  "decode_128 is measured.",
  "Sustained 258V throughput is proven.",
  "Arc 140V or Intel NPU performance is proven.",
]

[[work_item]]
id = "CPU258V-006"
status = "merged"
pr = 4001
branch = "codex/intel-258v-platform/CPU258V-006-warm-phase"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-005"]
acceptance = "Add a strict CPU warm phase runner that loads the 258V BitNet GGUF model/tokenizer once, emits per-profile strict CPU receipts for prefill_512 and decode_128, preserves selected backend/kernel and fallback=false, and keeps phase evidence separate from speedup, Arc, or NPU claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/tests/cli_arg_tests.rs crates/bitnet-bench-receipts/src/bin/cpu_phase_benchmark_receipt.rs --edition 2024",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --test cli_arg_tests cpu_phase_warm_session -- --nocapture",
  "cargo test --locked -p bitnet-bench-receipts --bin cpu_phase_benchmark_receipt --no-default-features tests:: -- --nocapture",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- -D warnings",
  "cargo clippy --locked -p bitnet-bench-receipts --no-default-features --bin cpu_phase_benchmark_receipt -- -D warnings",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/tests/cli_arg_tests.rs",
  "crates/bitnet-bench-receipts/src/bin/cpu_phase_benchmark_receipt.rs",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "A CPU-only warm phase runner can emit strict CPU phase profile receipts after merge.",
  "The converter preserves the larger measured prefill receipt when decode and prefill receipts are supplied together.",
]
must_not_claim = [
  "prefill_512 or decode_128 is measured without a real emitted profile receipt.",
  "Sustained 258V throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
  "CPU phase timings prove answer quality or acceleration.",
]

[[work_item]]
id = "CPU258V-007"
status = "merged"
pr = 4006
branch = "codex/intel-258v-platform/CPU258V-007-answer-template-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-006"]
acceptance = "Record the first 258V AVX2 answer-corpus refresh using the BitNet.cpp answer-ready prompt envelope, preserving timeout rows and missing child receipts as blocker evidence without answer-quality, parity, speed, Arc, or NPU claims."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-avx2-bitnetcpp-template.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The 258V AVX2 answer-corpus refresh under the BitNet.cpp answer-ready prompt envelope timed out within the bounded local child-run window.",
]
must_not_claim = [
  "The BitNet.cpp answer-ready prompt envelope improves 258V answer quality.",
  "Scalar/AVX2 parity under the new prompt is proven.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-008"
status = "merged"
pr = 4008
branch = "codex/intel-258v-platform/CPU258V-008-answer-case-filter"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-007"]
acceptance = "Add an answer-corpus case-id filter so 258V answer-template refreshes can run one bounded corpus case at a time, preserving full corpus identity plus selected case IDs in the aggregate receipt without answer-quality, parity, speed, Arc, or NPU claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/commands/answer_corpus.rs crates/bitnet-cli/tests/cli_arg_tests.rs crates/bitnet-cli/src/main.rs --edition 2024",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --test cli_arg_tests answer_corpus_case -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli --test cli_arg_tests answer_corpus_subcommand_help -- --exact --nocapture",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- -D warnings",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/commands/answer_corpus.rs",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/tests/cli_arg_tests.rs",
  "ci/hardware/intel-258v/README.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "answer-corpus can run selected case IDs and records selected_case_count plus selected_case_ids in the aggregate receipt.",
  "258V answer-template refresh diagnostics can be narrowed to one corpus case at a time after merge.",
]
must_not_claim = [
  "The selected case completed on the local 258V.",
  "The BitNet.cpp answer-ready prompt envelope improves 258V answer quality.",
  "Scalar/AVX2 parity under the new prompt is proven.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-009"
status = "merged"
pr = 4010
branch = "codex/intel-258v-platform/CPU258V-009-single-case-answer"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-008"]
acceptance = "Record a bounded 258V AVX2 answer-corpus attempt for a single BitNet.cpp-template case selected with --case-id, preserving the timeout row and missing child receipt as blocker evidence without answer-quality, parity, speed, Arc, or NPU claims."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-avx2-bitnetcpp-template-math_2_plus_2.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "ci/hardware/intel-258v/README.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The selected 258V AVX2 math_2_plus_2 BitNet.cpp-template answer-corpus case timed out within the bounded local child-run window.",
  "The aggregate receipt preserves full corpus identity and selected_case_ids for the single-case attempt.",
]
must_not_claim = [
  "The selected case completed on the local 258V.",
  "The BitNet.cpp answer-ready prompt envelope improves 258V answer quality.",
  "Scalar/AVX2 parity under the new prompt is proven.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-010"
status = "merged"
pr = 4012
branch = "codex/intel-258v-platform/CPU258V-010-release-single-case"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-009"]
acceptance = "Record a release-built 258V AVX2 answer-corpus attempt for the selected math_2_plus_2 BitNet.cpp-template case, preserving the completed strict CPU receipt row and quality failure evidence without parity, speed, Arc, or NPU claims."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-avx2-bitnetcpp-template-math_2_plus_2-release.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "ci/hardware/intel-258v/README.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The selected release-built 258V AVX2 math_2_plus_2 answer-corpus case completed within the bounded child-run window.",
  "The completed strict CPU row used real_gguf loading, explicit strict tokenizer resolution, selected i2_s-avx2-reference kernel, and fallback_used=false.",
  "The selected case failed the exact answer-quality gate with recorded generated token IDs and decoded text.",
]
must_not_claim = [
  "The selected case answered correctly.",
  "Scalar/AVX2 parity under the new prompt is proven.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-011"
status = "merged"
pr = 4014
branch = "codex/intel-258v-platform/CPU258V-011-release-scalar-case"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-010"]
acceptance = "Record release-built 258V scalar and scalar-vs-AVX2 answer parity artifacts for the selected math_2_plus_2 BitNet.cpp-template case, showing the bad generated answer is shared by scalar and AVX2 without answer-quality, speed, Arc, or NPU claims."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-scalar-bitnetcpp-template-math_2_plus_2-release.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-parity-bitnetcpp-template-math_2_plus_2-release.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "ci/hardware/intel-258v/README.md",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The selected release-built 258V scalar math_2_plus_2 answer-corpus case completed within the bounded child-run window.",
  "Scalar and AVX2 selected kernels generated identical prompt token IDs, generated token IDs, and decoded text for the selected case.",
  "The shared output still fails the exact answer-quality gate.",
]
must_not_claim = [
  "The selected case answered correctly.",
  "Full corpus scalar/AVX2 parity is proven under the new prompt.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-012"
status = "merged"
pr = 4022
branch = "codex/intel-258v-platform/CPU258V-012-gamma-rootcause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-011"]
acceptance = "Correct BitNet b1.58 CPU model mechanics to use RMSNorm and ReLU2, fix canonical BitNet tied-output-head receipt metadata, and record a strict one-token scalar fixture showing the answer-quality issue remains shared after the mechanics correction."
commands = [
  "cargo test --locked -p bitnet-common --test arch_defaults_validation_tests -- --nocapture",
  "cargo test --locked -p bitnet-common --test arch_config_consistency_tests -- --nocapture",
  "cargo test --locked -p bitnet-common --test rope_gqa_validation_tests -- --nocapture",
  "cargo test --locked -p bitnet-models --lib config_detection::tests::hf_config_bitnet -- --exact --nocapture",
  "cargo test --locked -p bitnet-models --lib config_detection::tests::gguf_detect_bitnet -- --exact --nocapture",
  "cargo test --locked -p bitnet-models --lib architecture::tests::defaults_bitnet -- --exact --nocapture",
  "cargo test --locked -p bitnet-transformer --lib tests::test_relu2_activation_squares_positive_values -- --exact --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- -D warnings",
  "rustfmt --check --config skip_children=true crates/bitnet-cli/src/main.rs crates/bitnet-common/src/arch_registry.rs crates/bitnet-common/src/config.rs crates/bitnet-common/tests/arch_defaults_validation_tests.rs crates/bitnet-models/src/architecture.rs crates/bitnet-models/src/config_detection.rs crates/bitnet-transformer/src/lib.rs --edition 2024",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-strict-raw-fixture-scalar-2plus2-relu2-rmsnorm.json",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-common/src/arch_registry.rs",
  "crates/bitnet-common/src/config.rs",
  "crates/bitnet-common/tests/arch_defaults_validation_tests.rs",
  "crates/bitnet-models/src/architecture.rs",
  "crates/bitnet-models/src/config_detection.rs",
  "crates/bitnet-transformer/src/lib.rs",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/src/formats/gguf/loader.rs",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "BitNet b1.58 architecture defaults now route through RMSNorm and ReLU2 for CPU decode construction.",
  "The transformer FFN now honors configured activation instead of always applying SiLU.",
  "Canonical BitNet run receipts now record tied token embeddings instead of an absent output.weight.",
  "The strict scalar one-token raw fixture still produces a bad token after the mechanics correction, so the shared answer-quality issue remains open.",
]
must_not_claim = [
  "The selected case answered correctly.",
  "Answer quality is fixed.",
  "Scalar/AVX2 parity changed.",
  "Sustained throughput is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-013"
status = "merged"
branch = "codex/intel-258v-platform/CPU258V-013-warm-phase-artifacts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-012"]
acceptance = "Record release-built 258V warm-session strict CPU phase receipts for prefill_512 and decode_128 after the BitNet b1.58 mechanics correction, preserving real GGUF loading, explicit tokenizer resolution, selected i2_s-avx2-reference kernel, fallback=false, and phase timing claim boundaries."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-phase-warm-session.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-phase-warm-session-profiles/prefill_512.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-phase-warm-session-profiles/decode_128.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The release-built 258V warm-session run emitted strict CPU phase receipts for prefill_512 and decode_128.",
  "The run used real_gguf loading, explicit strict tokenizer resolution, selected i2_s-avx2-reference, and fallback_used=false.",
  "prefill_512 and decode_128 are now backed by emitted warm-session profile receipts for the 258V CPU lane.",
]
must_not_claim = [
  "Answer quality is fixed.",
  "Sustained throughput is proven.",
  "A speedup is proven.",
  "Arc 140V or Intel NPU execution is proven.",
  "The phase receipts are accelerator comparison evidence.",
]

[[work_item]]
id = "CPU258V-014"
status = "merged"
branch = "codex/intel-258v-platform/CPU258V-014-answer-quality-triage"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-012", "CPU258V-013"]
acceptance = "Record post-mechanics release-built 258V scalar and AVX2 answer-corpus receipts for the selected math_2_plus_2 BitNet.cpp-template case, showing exact answer-gate pass and scalar-vs-AVX2 parity after the RMSNorm/ReLU2 and tied-output-head corrections without general chat, speed, Arc, or NPU claims."
commands = [
  "target/release/bitnet answer-corpus --model C:/Code/Models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer C:/Code/Models/BitNet-b1.58-2B-4T/tokenizer.json --device cpu --threads 8 --cpu-kernel scalar --case-id math_2_plus_2 --per-prompt-timeout-seconds 240 --dump-logit-steps 1 --logits-topk 5 --json-out target/cpu-answer-corpus-post-mechanics-scalar-math_2_plus_2.json",
  "target/release/bitnet answer-corpus --model C:/Code/Models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer C:/Code/Models/BitNet-b1.58-2B-4T/tokenizer.json --device cpu --threads 8 --cpu-kernel avx2 --case-id math_2_plus_2 --per-prompt-timeout-seconds 240 --dump-logit-steps 1 --logits-topk 5 --json-out target/cpu-answer-corpus-post-mechanics-avx2-math_2_plus_2.json",
  "target/release/bitnet answer-parity --scalar target/cpu-answer-corpus-post-mechanics-scalar-math_2_plus_2.json --avx2 target/cpu-answer-corpus-post-mechanics-avx2-math_2_plus_2.json --json-out target/cpu-answer-parity-post-mechanics-math_2_plus_2.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-scalar-bitnetcpp-template-math_2_plus_2-post-mechanics.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-avx2-bitnetcpp-template-math_2_plus_2-post-mechanics.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-parity-bitnetcpp-template-math_2_plus_2-post-mechanics.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The selected 258V scalar math_2_plus_2 BitNet.cpp-template case now passes the exact answer gate after the BitNet b1.58 mechanics correction.",
  "The selected 258V AVX2 math_2_plus_2 BitNet.cpp-template case now passes the exact answer gate after the BitNet b1.58 mechanics correction.",
  "Scalar and AVX2 generated identical prompt token IDs, generated token IDs, decoded text, and first-step top-logit evidence for the selected case.",
]
must_not_claim = [
  "General chat quality is proven.",
  "The full answer corpus is proven.",
  "Sustained throughput is proven.",
  "A speedup is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]

[[work_item]]
id = "CPU258V-015"
status = "pr_open"
pr = 4046
branch = "codex/intel-258v-platform/CPU258V-015-post-mechanics-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU258V-014"]
acceptance = "Record post-mechanics release-built 258V scalar and AVX2 answer-corpus receipts for the full committed strict-bitnet-answer-corpus-v1 prompt set, showing all five fixed cases pass and scalar-vs-AVX2 full-corpus parity holds after the RMSNorm/ReLU2 and tied-output-head corrections without general chat, speed, Arc, or NPU claims."
commands = [
  "target/release/bitnet answer-corpus --model C:/Code/Models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer C:/Code/Models/BitNet-b1.58-2B-4T/tokenizer.json --device cpu --threads 8 --cpu-kernel scalar --per-prompt-timeout-seconds 300 --dump-logit-steps 1 --logits-topk 5 --json-out target/cpu-answer-corpus-post-mechanics-scalar-full.json",
  "target/release/bitnet answer-corpus --model C:/Code/Models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer C:/Code/Models/BitNet-b1.58-2B-4T/tokenizer.json --device cpu --threads 8 --cpu-kernel avx2 --per-prompt-timeout-seconds 300 --dump-logit-steps 1 --logits-topk 5 --json-out target/cpu-answer-corpus-post-mechanics-avx2-full.json",
  "target/release/bitnet answer-parity --scalar target/cpu-answer-corpus-post-mechanics-scalar-full.json --avx2 target/cpu-answer-corpus-post-mechanics-avx2-full.json --json-out target/cpu-answer-parity-post-mechanics-full.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-scalar-bitnetcpp-template-full-post-mechanics.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-corpus-avx2-bitnetcpp-template-full-post-mechanics.json",
  "Parse ci/hardware/intel-258v/2026-05-08/cpu-answer-parity-bitnetcpp-template-full-post-mechanics.json",
  "Parse docs/tracking/campaigns/intel-258v-platform/active.toml",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/tracking/campaigns/intel-258v-platform/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
]
may_claim = [
  "The 258V scalar full committed strict-bitnet-answer-corpus-v1 run passes all five fixed BitNet.cpp-template answer gates after the BitNet b1.58 mechanics correction.",
  "The 258V AVX2 full committed strict-bitnet-answer-corpus-v1 run passes all five fixed BitNet.cpp-template answer gates after the BitNet b1.58 mechanics correction.",
  "Scalar and AVX2 full-corpus answer parity passes with summary.failed=0 and no first divergence for the fixed corpus.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Sustained throughput is proven.",
  "A speedup is proven.",
  "Arc 140V or Intel NPU execution is proven.",
]
