timestamp = "2026-05-09T13:32:08Z"
campaign = "apple-m4-slm-excellence"
item = "M4-SLM-EX-003"
event = "pr_open"
actor = "codex"
pr = 4284
head_sha = "6fce261d6de188d6a49da66e54eb7b7a53a190bd"

notes = [
  "Opened PR #4284 for M4-SLM-EX-003.",
  "The PR reuses the already verified Mac model-cache SHA in resident warm-session receipts instead of rehashing the large GGUF again before generation.",
  "The PR records `model.sha256_source`, `model.sha256_rehash_skipped`, and `timing.model_sha256_ms` so the TTFT/cache-verification placement improvement is auditable.",
  "The PR also normalizes the observed leading Qwen assistant separator before quality prefix gates while preserving generated text and token IDs in receipts.",
  "No BitNet local-answer quality, full Apple Metal inference, QK256, Neural Engine, MPSGraph model inference, MacBook work, or broad M4 performance claim is made.",
]
