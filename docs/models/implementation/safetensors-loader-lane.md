# Safetensors loader lane

This lane records future safetensors loader rails: model index discovery, tensor-name mapping, dtype policy, sharding, tokenizer/template sibling files, and hash receipts.

A safetensors loader scaffold is not an inference claim until a named variant/backend/task has a receipt.

