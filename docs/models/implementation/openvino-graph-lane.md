# OpenVINO graph/reference lane

This lane is for graph-level reference artifacts, not native kernel support. Receipts must identify the IR/ONNX source, hash, requested backend, selected backend, fallback state, task, and whether the result is an external reference rather than native bitnet-rs inference.

