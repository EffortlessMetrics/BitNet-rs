# Multimodal lane

Multimodal support is separate from text-only decoder support. A text-only proof for a multimodal family must set `multimodal_claim=false`.

Required future rails include modality preprocessing, projector or encoder artifacts, input shape receipts, template handling for mixed content, and separate image/audio/video proof receipts.

