# Architecture feature glossary

Anything not source-backed must be marked `TBD`, not inferred. Family docs reference these terms rather than redefining them.

## `dense_decoder`

Planning term for dense decoder. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `moe_decoder`

Planning term for moe decoder. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `effective_parameters`

Planning term for effective parameters. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `active_parameters`

Planning term for active parameters. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `hybrid_thinking`

Planning term for hybrid thinking. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `thinking_mode`

Planning term for thinking mode. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `reasoning_effort`

Planning term for reasoning effort. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `developer_role`

Planning term for developer role. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `tool_calling`

Planning term for tool calling. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `structured_outputs`

Planning term for structured outputs. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `multimodal_text_image`

Planning term for multimodal text image. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `multimodal_audio`

Planning term for multimodal audio. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `video_as_frames`

Planning term for video as frames. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `vision_encoder`

Planning term for vision encoder. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `audio_encoder`

Planning term for audio encoder. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `per_layer_embeddings`

Planning term for per layer embeddings. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `shared_kv_cache`

Planning term for shared kv cache. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `sliding_window_attention`

Planning term for sliding window attention. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `global_attention`

Planning term for global attention. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `p_rope`

Planning term for p rope. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `yarn`

Planning term for yarn. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `long_context`

Planning term for long context. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `compressed_sparse_attention`

Planning term for compressed sparse attention. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `heavily_compressed_attention`

Planning term for heavily compressed attention. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `mamba_or_hybrid_state_space_tbd`

Planning term for mamba or hybrid state space tbd (TBD). A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `mtp_drafter`

Planning term for mtp drafter. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `eagle_drafter`

Planning term for eagle drafter. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `gguf_dynamic_quant`

Planning term for gguf dynamic quant. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `mxfp4`

Planning term for mxfp4. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `fp4_fp8_mixed`

Planning term for fp4 fp8 mixed. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `token_classification`

Planning term for token classification. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `bioes_labels`

Planning term for bioes labels. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.

## `viterbi_span_decoder`

Planning term for viterbi span decoder. A source-backed family doc must still state the exact source-backed behavior and any unknowns as `TBD`.


