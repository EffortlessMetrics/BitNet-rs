//! SafeTensors format loader

use bitnet_common::Result;

/// SafeTensors format loader (placeholder)
pub struct SafeTensorsLoader;

impl SafeTensorsLoader {
    pub fn new() -> Self {
        Self
    }
    
    pub fn load(&self, _data: &[u8]) -> Result<()> {
        // Placeholder implementation
        Ok(())
    }
}