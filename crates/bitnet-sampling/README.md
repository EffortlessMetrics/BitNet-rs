# bitnet-sampling

Sampling strategies for BitNet LLM text generation.

Provides `SamplingConfig` and `SamplingStrategy` with temperature scaling, top-k, top-p (nucleus), and repetition penalty sampling.
