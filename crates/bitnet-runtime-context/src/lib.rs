//! Compatibility façade over `bitnet-runtime-context-core`.
//!
//! This crate keeps existing downstream API stable while the core context
//! resolution logic lives in the dedicated microcrate.

pub use bitnet_runtime_context_core::*;
