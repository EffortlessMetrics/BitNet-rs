# bitnet-qk256-layout-core

SRP microcrate that owns QK256 tensor layout parsing and input shape validation.

This crate is intentionally pure and Candle-independent so it can be reused by
multiple runtime/dispatch crates.
