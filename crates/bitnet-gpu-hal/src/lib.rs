//! GPU hardware abstraction layer for `BitNet` inference.
//!
//! Provides a cross-backend kernel test harness for validating GPU kernel
//! correctness across different compute backends.

pub mod test_harness;
