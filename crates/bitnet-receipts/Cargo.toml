[package]
name = "bitnet-receipts"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
description = "Inference receipt generation and verification for BitNet (schema v1.0.0)"
categories.workspace = true
keywords.workspace = true
readme = "README.md"
repository.workspace = true
rust-version.workspace = true


[lints]
workspace = true
[dependencies]
bitnet-receipts-core = { path = "../bitnet-receipts-core", version = "0.2.1-dev", default-features = false }

[features]
gpu = ["bitnet-receipts-core/gpu"]
cuda = ["bitnet-receipts-core/cuda"]

[dev-dependencies]
proptest = { workspace = true }
insta = { workspace = true }
tempfile = { workspace = true }
serde_json = { workspace = true }
bitnet-common = { path = "../bitnet-common", version = "0.2.1-dev" }
