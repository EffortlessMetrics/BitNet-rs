# bitnet-rope

Shared rotary position embedding (RoPE) cache table generation for BitNet crates.

This crate has a single responsibility: generate deterministic sine/cosine lookup tables used by RoPE implementations.
