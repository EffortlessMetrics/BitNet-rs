# CPU Proof Goal

Make real BitNet CPU inference strict, receipt-backed, and measurable without routing around BitNet model, tokenizer, layout, or fallback truth.

Work from loader authority and tokenizer authority into packed layout, scalar truth kernels, SIMD dispatch, transformer decode coverage, strict receipts, and benchmarks. Do not claim CPU performance until strict real-model receipts prove the selected kernel path ran without fallback.
