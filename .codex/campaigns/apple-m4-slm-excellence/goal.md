Turn the working Apple M4 dense SLM path into an appliance-grade local model runner experience. Keep the scope on the M4 Mac mini: improve `bitnet mac ask`, `bitnet mac chat`, `bitnet mac smoke`, local health checks, perceived latency, allocation hygiene, quality coverage, long resident sessions, regression reporting, and operator expectation docs for the verified dense Qwen Apple CPU/NEON path. Preserve the separation between dense SLM evidence and BitNet proof. Do not claim BitNet quality, full Apple Metal inference, Neural Engine execution, MPSGraph model inference, QK256 support, or broad Apple Silicon performance. Do not commit model binaries.
