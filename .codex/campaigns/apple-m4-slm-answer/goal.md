Make the M4 Mac mini produce prompt-in, intelligible-answer-out behavior with a small, storage-conscious dense SLM while keeping the BitNet Apple local-answer gates blocked until a BitNet artifact is reference-good. Preserve the same receipt discipline as the BitNet lanes: model identity, tokenizer authority, requested and selected backend, fallback status, generated text, token IDs, timing, and explicit claim boundaries. Start with CPU/NEON answers and warm-session usability; add Metal only after a receipt-backed phase contribution preserves greedy output.
