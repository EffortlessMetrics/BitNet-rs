Turn the measured Apple M4 dense SLM local-answer envelope into repeatable regression guardrails. Compare only matching Qwen2.5 dense SLM release receipts with the same model, tokenizer, backend, fallback status, profile, and machine context. Treat quality, determinism, receipt schema, model-cache identity, and fallback mismatches as correctness failures before interpreting timing drift. Keep timing thresholds advisory until repeated scheduled Apple hardware receipts exist. Do not claim BitNet local-answer quality, full `apple-m4-metal` inference, Neural Engine execution, MPSGraph model inference, QK256 support, or broad M4 performance from this lane.
