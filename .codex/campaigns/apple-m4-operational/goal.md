# Apple M4 Operational Goal

Turn the completed Apple M4 proof lane into a repeatable operator workflow with one-command validation, durable receipts, clear docs, stable failure modes, and benchmark profiles.

Keep `apple-m4-cpu-neon`, `apple-m4-metal`, and `apple-m4-mpsgraph` separate. Do not claim full Metal model inference, Neural Engine execution, QK256 support, or general M4 performance unless a strict receipt-backed item explicitly proves that claim.
