# Pinned Rust toolchain for reproducible builds
# MSRV: 1.89.0 (using Rust 2024 edition)
[toolchain]
channel = "1.89.0"
profile = "minimal"
components = ["rustfmt", "clippy", "rust-analyzer"]
targets = []